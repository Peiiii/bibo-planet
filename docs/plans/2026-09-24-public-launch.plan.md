# Bibo Planet 公网上线与验收记录

状态：进行中。本文是本轮交付的长期记录；遇到上下文压缩，先以这里的验收合同和当前证据继续，不把未验证事项当成完成。

## 目标纠偏与当前工作（2026-09-25）

用户澄清「可获客」是**成品达到可以交给陌生用户使用并有吸引力的质量门槛**，不是授权或要求本任务实际拉新、投放、商业运营。不能再把我们是否开展获客/运营当作产品功能完成条件；必要的数据处理、安全、可用性边界仍需如实说明，不能因此假装已完成。用户同时指出当前 AI 连自己的文件也无法操作，这与原构想的「每个 AI 有自己的工作区、能逐步改造自己」不符。现网截至上一检查点仍仅有两项共享笔记工具；刚部署的只是一项首屏加载修复，**不是文件能力发布**。

本批选择开放三位 AI 各自的持久文本文件列目录/读/写，不开放宿主文件、命令、网络或 mini-app，并把真正的文件能力列为独立 AC-13。核心产品旅程和逐条失败判定见[共享 AI 产品设计的黄金验收](../designs/2026-09-25-shared-ai-product.design.md#产品交付的黄金验收链路)，这五条与下方 AC 共同构成交付标准。

## 当前接续检查点（2026-09-25 01:26 北京时间）

**AI 自有文本文件能力已真实上线并完成公网黄金链路 3；整站产品交付标准仍未全过。** Bibo `25c6581` 已推送 GitHub、ECS 同 SHA，Cloudflare Worker `85460cac-1093-4f28-9542-118f5386823d`、静态 JS `index-BdR_WfRE.js`。NextClaw SDK 快照未变；三只 Agent 仍走真实 `runTask`，仅五项 Bibo 工具可用。工作区根目录可实际列出 `AGENTS.md`、`IDENTITY.md`、`MEMORY.md`、`files/`，`files/` 内可读/写有限 UTF-8 文本；宿主、其他 AI、命令、网络和 mini-app 未开放。发布前 46/46 测试、TypeScript、lint、格式、构建、Wrangler dry-run、差异检查通过；定向测试覆盖穿越/软链接/容量/跨 AI，删除重放覆盖「正常重启保留新内容」与「旧备份覆盖恢复也重新清理」。本地隔离真实 Agent 写→读→列目录成功。

公网两个隔离 Chrome 账号经正常界面完成：甲向墨里问当前目录并得到真实工具结果，再让它写入且读回一份非私人 QA 文本；刷新后会话仍在。乙从另一浏览器账号让墨里读同一文件，实际得到一致内容；乙换皮可读取则收到 `ENOENT`，没有甲的文件内容。390px 手机 `innerWidth=scrollWidth=390`。第一次浏览器自动验收脚本因切换 AI 后复用上一会话的消息计数而误超时，服务端四次 Agent 调用均完成；随后**复用相同测试账号做页面只读核对**，四段回复都与真实文件状态匹配，没有重建账号或再调用模型。公开首页/世界 API/原站 200，无凭据源站 403。独立检查点与完整对象见[运行手册](../OPERATIONS.md)。

更新前 Bibo 专用加密备份在 OSS 可见，密文 SHA 与服务器一致并可流式解出完整目录。文件产生后再次运行同一备份 service，Bibo 重启后文件与账号会话仍在；新离机对象 `daily/daily-2026-09-25-012450.sE62Y5d8.tar.gz.gpg` 可解出账号、三只 AI 状态和新增文件，密文 SHA 与 ECS 原件一致。本轮没有生产原位覆盖恢复。备份 timer 仍 active、`LastTriggerUSec` 为空，首次自然触发预计 04:16:53 CST；已知 DeepSeek 共享钱包只读检查仍可用且在此前 1–10 元区间，没有充值或把它冒称 Bibo 独立预算。AC-13 按文本文件范围通过；AC-05/06/08/10/11 及 AC-12 的剩余安全/恢复边界继续开放。下一轮优先观察自然定时备份并按黄金链路 1、2、4、5 做第三轮产品与可靠性审查；**不开展实际获客或商业运营**。

## 当前接续检查点（2026-09-25 01:55 北京时间）

**公网首屏加载失败恢复已修复并发布；整体正式获客目标仍未完成。** 发现前端首次 `/api/world` 请求若悬挂，`refreshing` 一直为 true，30 秒刷新也不能重试，而错误只会出现在手机首屏以下。公网连续 8 次匿名读均为 200、约 0.9–1.8 秒，不能据此声称服务平时经常悬挂；修的是失败边界。按[公网设计](../designs/2026-09-24-public-world.design.md#首屏世界列表的悬挂请求恢复2026-09-25)只给世界列表读取设 20 秒终止，在列表原位提供重试，不给模型写操作统一加超时。Bibo `12a6267` 已推送 GitHub，Cloudflare Worker `df31f70d-3ac2-4213-9d9f-d25337156a7f` 上线；ECS Agent 后端和同机旧站未重启。44/44 测试、tsc、定向 lint、格式、构建、Wrangler dry-run 与差异检查通过。本地 390px 浏览器拦住 `/api/world`：20.2 秒退出 loading、出现可访问错误/重试，放行后约 0.5 秒回到三张卡片；**同一故障在真实公网页面**约 21.6 秒出现错误，放行并重试约 1 秒恢复，页面无横向溢出。公网首页、世界/会话 API、原站 200，无密钥回源 403；没有创建账号或额外消耗模型。这个局部可靠性修复不能关闭 AC-05/06/10/11/12 或第三轮整体审查。详情见[运行手册](../OPERATIONS.md)。

## 当前接续检查点（2026-09-25 00:40 北京时间）

**当前公网 Agent 的真实浏览器两轮链路已补验，整体正式获客目标仍未完成。** 使用全新 Chrome headless 资料从 `https://planet.bibo.bot/` 的注册弹窗创建合成 QA 账号，未从 API 绕过界面；在墨里对话区连续发送两条简短测试消息，页面收到两条真实模型回复，账号保护的会话接口从 0→2→4 条。第二轮回复准确识别第一轮的“学吉他”事实及当前账号昵称。忽略缓存刷新后，账号仍登录、会话仍为 4 条，390×844 移动视口 `innerWidth=scrollWidth=390`；点击墨里卡片可滚到聊天区，第二轮问答在页面正常显示，没有横向溢出。本轮只创建一个合成账号、执行两次模型对话，未做多余调用；DeepSeek 共享账号余额只读复核为可用且仍在已知 1–10 元区间，未输出密钥/具体金额，也未充值。

这关闭了 00:08 检查点所列的**新版浏览器注册→连续对话→刷新恢复**缺口，但不能由单账号推导所有跨账号、对抗输入、故障或生产原位恢复通过。匿名手机先写草稿→触发注册且草稿保留也已在另一份全新浏览器资料中补验。00:42 通过 ECS 云助手只读查看实际 systemd 状态：`bibo-planet-backup.timer` 仍 active、`LastTriggerUSec` 为空，下一次为 2026-09-25 04:18:56 CST；备份 service 最近结果 success 仍是此前手动运行，**自然触发尚未发生**。本任务 Codex heartbeat 配置为每天 4 次只读查余额并在不可用、低于 1 元或异常下降时告警；配置为 active 不证明首次调度成功，且共享钱包监测不等于 Bibo 独立账单或硬停机限额。AC-05 的独立费用边界、AC-10/11 的运营者/保存期限/适用要求与 AC-12 的剩余安全和删除范围继续开放。产品能力还需按[共享 AI 产品设计草案](../designs/2026-09-25-shared-ai-product.design.md)由用户评审；当前两项笔记工具不能冒充可自由访问文件或自建应用。

## 当前接续检查点（2026-09-25 00:08 北京时间）

**Bibo 公网后端已切换为受限 NextClaw Agent；整个“可直接获客成品”目标仍未完成。** Bibo Git `c0d7b1d` 推送至 `origin/master`、ECS 同 SHA；NextClaw SDK 源码分支 `codex/bibo-agent-tool-policy` 已推送至 `651fe5b25`。Cloudflare Worker/前端未变，线上入口仍为 <https://planet.bibo.bot/>。服务器运行身份下隔离启动、真实 SDK 包导入与 TypeScript 检查通过，只有 Bibo 服务/单元被更新，同机旧站仍为 200。运行期内存约 285 MiB、`NRestarts=0`。两个新账号公网真实 Agent 三轮，甲识别自己的昵称及“学吉他”历史，乙识别自己的昵称且未错认甲；两人私聊 4/2 条，每轮模型调用 1 次，临时运行目录没有 session/SQLite 文件。随后 Bibo 专用加密备份上传、精确取回并流式解密，新增的三只 Agent 工作空间都在归档中；备份服务重启使临时目录归零。重启后第三个新账号通过公网皮可 Agent 一轮，昵称识别、报告 1,178 tokens、会话 2 条均成立。该证据关闭了“线上还是 `models.chat`、不知道当前用户”的直接缺口，但只覆盖受控公开测试账号、正常完成和重启路径，不是对所有对抗输入及恢复场景的证明。

浏览器自动控制两次超时，前端源码未改，先前交互证据仍是旧后端时的 UI 证据。2026-09-25 00:24–00:27 北京时间改用本机全新、未登录的 Chrome headless 直接打开公网：1440×900 桌面截图里三只 AI 卡片、模型资料、额度、注册入口和完整对话输入框均正常渲染；390×844 手机以 DevTools 移动视口加载后 `innerWidth=scrollWidth=390`，三张卡片可见，点墨里卡片会滚到对话区，输入匿名草稿并点发送会打开「创建账号」弹窗，原草稿仍在输入框。整个检查没有注册账号或调用模型。这补的是**当前公网匿名桌面/手机渲染与注册入口**，不证明新版 Agent 的登录、连续对话、共享与恢复的浏览器链路。AC-12 记为**线上主链路通过，完整验收待浏览器与剩余安全/删除范围复核**。AC-06 的第一次自然定时备份计划约 04:15 北京时间，尚未发生；AC-10/11 的备份保留期、公开运营信息、数据权利路径和适用监管条件需要用户选择/外部核定，不能因为 Agent 可用就声称已经可正式获客。共享供应商余额此前只读检查仍在已知 1–10 元区间，没有自行充值；这是共享钱包，不是 Bibo 专属计费硬上限。详细生产锚点和备份对象见[运行手册](../OPERATIONS.md)。

## 当前接续检查点（2026-09-24 23:55 北京时间）

**公网仍未切换，不能声称 Agent 已上线。** NextClaw 隔离分支 `codex/bibo-agent-tool-policy` 的收敛补丁已提交并推送，源码 SHA `651fe5b259178abb47d8c394b7b3840978c21e71`；Bibo 的 19 包 vendored 快照来自这一源码及同一工作区依赖闭包。重新构建并打包后，19 个 tarball SHA-512 与锁文件逐一匹配，`pnpm install --frozen-lockfile --offline` 通过。Bibo 44/44 测试、`tsc`、lint、格式、构建和差异空白检查通过；NextClaw Kernel/Harness `tsc`、Kernel 定向 20/20 测试和差异可维护性检查通过（0 error，1 个 400 行文件预算提醒）。上一检查点的“最终全套待重跑/源分支待推送”已完成；真实 Flash 两账号结果仍以该节记录为准，尚未冒充新的公网测试。

下一步：在远端先只读核对 Bibo 与同机旧站、代码 SHA、内存及持久数据；执行 Bibo 专用加密备份并独立验证对象；再以服务身份验证固定 SDK 包安装、受限 Agent、临时目录与持久工作区权限，必要时回滚应用而不碰数据。切换后须以公网真实账号验证“知道当前昵称/只接续本人私聊”、模型用量、无宿主工具与失败路径，同时观察旧站健康。首次自然备份、隐私/运营与境内公开服务约束仍按 AC-06/10/11 独立验收，不能因 Agent 切流而宣布整个获客目标完成。

## 当前接续检查点（2026-09-24 23:48 北京时间）

**线上仍为 model-only，AC-12 未关闭；本节全部是本地隔离验证。** 用户要求优化此前的架构并指出 AI 不认识当前用户。查出 HTTP 已认证账号有 `id/name`，但运行时只接 ID 并向模型提供前 8 位编号；现已让完整的当前账号身份和本人与当前 AI 的私人历史直接进入 Agent 上下文，不把用户当 AI 主人，也不凭别的 AI 的未知私聊编造经历。Bibo 删除了模型消息→Agent 输入→伪造 `LLMResponse` 的适配回路，测试替身现在以 Agent 运行输入/结果工作。三只 AI 启动时只核验配置，逐轮按需创建 Harness，不留空闲实例；停服务会先取消并等待进行中的轮次。

NextClaw 隔离分支新增的收敛：受限工具目录遇到同名已允许工具立即失败，不再静默选先注册者；`contextProfile: "embedded"` 保留基础安全与执行约束而省去 NextClaw 产品专属上下文；`homeDir` 也拥有 asset 目录。真实 Agent 一轮揭示异步会话日志仍在写入时直接删除目录会出现 `ENOTEMPTY`，因此 Bibo 仍先 `sessions.delete` 等待该 session 事件并逻辑删除，Kernel dispose 也排空剩余事件并关闭会话目录，最后才物理删除临时 home；不能把逻辑删会话称为物理隐私保证。该竞态和停服务取消均已有定向测试。

NextClaw Harness 的实际 workspace 内部依赖闭包是 19 个包，不是 Kernel/Harness 两个包。已从同一隔离源码依赖图构建并打包为 Bibo `vendor/nextclaw-agent/*.tgz`（合计约 1.9 MB），`package.json` 用文件依赖和 pnpm overrides 固定完整闭包；19 个 tarball 的 SHA-512 与 `pnpm-lock.yaml` 全部匹配。Bibo 现已脱离 `pnpm link`。在全新临时目录只复制 package、lock 和 vendor 后，`pnpm install --frozen-lockfile` 与 Harness 模块导入成功；本仓库离线锁文件安装、`tsc`、44 项测试中的本轮定向项目（最终全套 44 项待收尾重跑）、lint、格式/构建此前 43 项版本均通过。包装形式是此轮不发布 NextClaw 约 43 包标准 NPM 批次的固定部署快照，长期须换成有版本号的正式 SDK 发行；源分支仍须提交/推送并记录 SHA。

正式包形态下的隔离 DeepSeek Flash 真 Agent 连续三轮成功，各轮实际模型调用 1 次。测试甲的第二轮准确说出当前昵称和上一轮“学吉他”，测试乙只说出自己的昵称，并明确不知道乙过去学过什么；私人历史分别 4/2 条，运行 home 完成后仅余无对话的 `configs` 目录。另用故意无效的合成 API Key 触发模型失败，未记私人消息且临时 home 仍只余 `configs`。这些证明了本机正常/供应商失败路径，不证明 ECS 身份、崩溃后 `/run` 清理、公网浏览器或法律运营门。共享 DeepSeek 钱包只读复核可用，余额仍在已报告的 1–10 元区间；没有充值或输出凭据。下一步先完成代码 Review 与最终回归，验证 ECS 的运行身份/内存/残留/备份，再决定能否切换公开服务；切流前绝不能把现网页面当成 Agent 已上线。

## 当前接续检查点（2026-09-24 22:29 北京时间）

**线上仍为 model-only，AC-12 未关闭。** Bibo 与 NextClaw 的 Agent 改动均只在本地、未提交或部署。完成逐轮销毁临时 Harness home 后，统一重跑 Bibo `tsc`、43/43 测试、lint、格式、Vite build、差异空白检查，均通过；NextClaw Kernel `tsc`、18/18 定向测试、Harness `tsc`/构建/公共入口测试通过，Kernel lint 只有 14 个既有 warning，差异维护性检查 0 error、2 warning。随机合成消息的本机临时运行目录扫描未发现 SQLite WAL 或其他剩余文件中的原文，但只覆盖正常完成路径。

又以两个全新账号通过本机真实 HTTP API 完成注册、各向同一只 AI 发送一条消息、读取各自会话。两次均由 `NextclawHarness.runTask` 实际执行受限 Agent，各 1 次模型调用、报告用量分别 853/954 tokens、回复非空；两个账号各自只看到自己的两条私聊记录。该检查证明了逐轮重建后仍可连续提供服务与私人会话隔离，不证明公网切流、长期稳定或完整隐私验收。一次测试昵称因包含 UUID 的连字符被注册规则拒绝，测试数据改为合法昵称后成功；不是产品代码故障。

另用带随机合成标记的真实 Agent 消息检查了持久工作空间：回复成功，`workspace/` 全目录扫描没有该标记；原始对话仍按设计保存在世界状态中。只说明当前正常路径没有把该轮原文额外复制进 Agent 工作空间，不是对所有故障路径或私聊泄露的形式化保证。22:29 对共享 DeepSeek 供应商钱包做只读查询，可用、CNY、余额仍落在此前已报告的 1–10 元区间；没有充值或输出具体金额，且该余额不是 Bibo 专属账单或硬预算。

`pnpm link` 仍使 Bibo 本机依赖状态偏离正式依赖，绝不能据此部署。NextClaw 标准 NPM changeset 批次已累计约 43 个包，发布整个批次明显超出 Bibo 本次所需的 Kernel/Harness 两个 SDK 包；已异步请用户选择发布范围。NextClaw SDK 来源已提交并推送到隔离分支 `codex/bibo-agent-tool-policy`，提交 `bc17e7780`，未合入主干或发布 NPM。

曾尝试仅将该提交的 Kernel/Harness 编译包固定到 Bibo，以避免整批发布；包可生成并干净安装，`tsc` 也通过，但真实运行时测试立即因 registry 中的旧 `@nextclaw/core` 缺少 `buildLocalizedTextMap` 导出失败。说明这两个包并非当前源码的完整运行时兼容闭包，**此构建已撤除、没有发布或部署**。继续只固定这两个包将是不可用的假交付；要么走经完整验证的 NextClaw 标准依赖批次，要么构建并验收完整匹配的内部依赖闭包。当前本机重新链接隔离源码用于开发；`pnpm-lock.yaml` 尚有包管理器机械改写，不可提交为生产锁文件。下一步先确定可发布依赖集合，再做 ECS 身份下的安全、内存、残留、恢复与公网链路验收；现网未切换。

## 前一接续检查点（2026-09-24 22:15 北京时间）

**线上仍是 model-only，AC-12 未关闭。** 本地 Bibo `master` 从已推送的 `362a012` 开始，只修改了本任务设计、Agent 运行时、共享记忆与删除测试、备份脚本和 unit；`pnpm link` 还机械改写了 `pnpm-lock.yaml`，须在发布前恢复为正式包版本，绝不能把相邻源码链接发布。NextClaw 的新 SDK 能力仍在 `/Users/peiwang/Projects/nextbot-bibo-agent-tool-policy` 独立 worktree，未提交、未发布；Nextbot 主工作区的他人草稿未触碰。

实现进展：Bibo 生产代码已不再注册 `ModelOnlyContribution` 或调用 `kernel.models.chat`。每只 AI 使用独立 `NextclawHarness.runTask`，静态只允许 `bibo_memory_read` / `bibo_memory_replace` 两个无路径参数的工具；斜杠命令不执行、原生冗长上下文/自动标题/会话搜索索引关闭，一次性 session 在 run 后删除。运行配置从零生成，只有选中供应商可用，密钥通过引用读取；NextClaw 自动补齐的内置供应商被显式禁用，只有不可用占位值，原无关供应商字面凭据不被复制。`WorldStore` 保留私人对话/共同遭遇唯一 owner，每只 AI 的 `MEMORY.md` 是受限、可在任意账号删除及离机墓碑重放时全部清空的共享补充；停服务备份脚本已纳入三只 AI 的工作空间。设计取舍见[公网设计](../designs/2026-09-24-public-world.design.md)顶部：初版不开放任意文件、命令、网络或代码执行，未来开放时另过 OS 隔离门；这并非已交付自编程 mini-app。

本地真实模型证据：本机 `.env` 选中的 `codex-sub` 指向 `127.0.0.1` 代理，连续调用在 45 秒内无任何模型输出，工具次数 0，**不能作为可用模型路径**；这不是 NextClaw Agent 运行入口缺失。显式选用已配置的 `deepseek/deepseek-flash` 后，受限 Agent 返回真实 `kind=agent`、run ID、模型回复和报告用量。关闭不适用的 NextClaw 原生上下文后，简单问候一次调用约 0.85 秒、841 tokens、1 次模型请求；在要求记公开测试事实的另一轮中，它实调 `bibo_memory_replace`，报告 3 次模型请求、3,054 tokens，记忆跨运行时重启仍在，删除访客数据后笔记和该访客私聊均清空。**这是隔离临时世界和本机凭据的验证，不是公网验证。**

验证：Bibo `tsc`、42/42 单测、lint、格式、Vite build 通过；NextClaw Kernel 定向 17/17 测试、`tsc`、lint（仅 14 个预存 warning）、Harness 公共入口测试/构建通过，差异维护性检查 0 error / 1 个 Kernel 文件临近 400 行 budget 的 warning。新增了旧快照 + 离机墓碑重放时共享笔记不复活的回归断言。尚待 SDK `maxTokens` 最新补丁的完整重验、正式 NPM 发布和独立安装、Bibo 正式依赖切换、ECS 私有数据快照、服务身份下的受限工具/权限/内存/会话残留验证，再经公网桌面/手机注册与连续对话复测。安全或模型成本门不通过则保留现网旧版，不声称切流成功；AC-05/06/08/10/11 等先前未关闭项依旧未关闭。

追加隐私验收（约 22:25，北京时间，仍未发布）：只删 NextClaw session 后，用随机合成标记扫描临时 Agent home，发现 `.ncp-agent-session-catalog.sqlite-wal` 仍含该轮原文。已改为每轮结束销毁整个临时 Harness home；持久 `workspace/agents/<id>` 独立保留、稳定 Agent ID 每轮重新加载。再次用真实 DeepSeek Flash 跑一轮，返回有效 Agent 回复、1 次模型请求、924 tokens；扫描剩余临时运行目录 19 个文件，合成标记残留 0 处。这个证据覆盖本机正常完成路径，不覆盖进程崩溃和 ECS systemd RuntimeDirectory 的实际行为，须在生产隔离验证。原上段 42 项与 17 项测试数字是当时快照；后续已扩展到 Bibo 43 项及 SDK 18 项，但最新整套检查尚待统一收尾。

## 当前接续检查点（2026-09-24 21:30 北京时间）

用户再次明确指出：目前没有真正使用 NextClaw Agent。核查结论是肯定的：线上 `SpiritRuntime` 注册 `ModelOnlyContribution`，只调用 `kernel.models.chat`；尚未调用 `NextclawHarness.runTask`，没有 Agent run、可操作的隔离工作区或持久能力演进。此前选择 model-only 是为避免把匿名公网输入直接接入 NextClaw 默认的宿主文件、命令和网络工具，但把它当成最终交付是不对的。**AC-12 仍开放**；接续工作必须实现和实测受限的真实 NextClaw Agent，不能把改文案、调用同一个模型或仅换成无工具的 `runTask` 宣称为完成。

已完成的局部纠偏：去除默认虚拟世界/角色扮演，界面说明改为现实中的多人共享 AI；后端根据问题相关性选择共同记录，普通身份/能力提问不再注入旧「星球」故事。Bibo Git `469e83b` 已推送并部署到 ECS；Cloudflare Worker 为 `0a3e79e2-63db-4251-a148-ade7683c60bc`。更新前专用备份服务成功，私有 OSS 精确对象 `daily/daily-2026-09-24-212407.es4oRER1.tar.gz.gpg` 为 18,656 字节，仅确认上传存在，未对这份对象做取回解密。远端 TypeScript、服务及 timer 状态通过，`NRestarts=0`；本地 41 项测试、TypeScript、lint、格式和构建通过。公网新合成账号真实问「你是谁？你能做什么？」得到完整、直白且无旧幻想叙事的模型答复；另一合成账号问早前的「蓝色风铃」线索，得到相关的跨用户共同记录。桌面与 320×700 手机公网界面可见，320px 无横向溢出，注册弹窗开关和焦点返回正常。合成账号及对话仍在生产状态；账号自删尚关闭，不得声称已清理。本轮证据仅关闭神秘叙事和无关共同记录污染，**不关闭 Agent、公开删除、独立费用监控、首个自然备份及正式获客验收**。

NextClaw Agent 安全迁移的本地进展（约 21:43，北京时间，**未发布**）：为保护 Nextbot 主工作区已有他人草稿，在相邻 `/Users/peiwang/Projects/nextbot-bibo-agent-tool-policy` 隔离 worktree 起草 Kernel/Harness 静态工具白名单。受限 Harness 不再把 `/help` 等访客文本作为 NextClaw 斜杠命令执行；一次性 session 增加完成后删除入口；修复自定义 `configPath` 下 `agents.create` 写入错误位置的缺陷并加回归测试。定向 15 项测试、Kernel/Harness TypeScript、Harness 公共包测试和差异维护性检查通过（后者有 Kernel 文件临近预算提醒）；本地隔离临时目录用现有供应商凭据实跑 `runTask`，返回 `kind=agent`、稳定 Agent ID 和 run ID、真实文字回复，session 随后删除，临时目录已清理。该冒烟使用**空工具集**，只证明真实 Agent run 主链路可运行，**尚未证明安全工作区读写、用户隔离、成本、备份/删除和公网部署**。下一步设计/实现 Bibo 专有的受控工作区工具，禁止宿主命令、外网和任意文件路径；通过安全与私人数据测试后才发布 SDK 和切流。当前公开版本仍是上段所述 model-only。

本地配置检查还发现：Bibo 专用 `nextclaw-config.json` 文件权限是 `0600`，但当前 `prepareHarnessHome()` 会保留其他供应商配置，其中存在与本轮模型无关的字面凭据。未输出凭据，也未把它复制到本地 Agent 冒烟的临时配置；冒烟只通过源文件引用获取目标供应商密钥。真实 Agent 切流前必须把 Bibo 生成配置缩到目标供应商与所需 secret reference，不能把无关凭据留在 Agent 可触及的运行目录。

## 历史接续检查点（2026-09-24 20:49 北京时间）

用户在公网体验后连续纠偏：AI 的神秘/星球角色扮演式说话不符合预期，应真实、直接；更关键的是，他要求每个共享 AI **实际运行 NextClaw Agent**。审查 `SpiritRuntime` 已确认线上当前只通过 `ModelOnlyContribution` 调用 `kernel.models.chat`，没有 NextClaw agent/session/runTask，不能称为已满足该要求。`@nextclaw/harness@0.2.24` 确有完整 Agent 入口，但默认工具包括宿主文件读写、命令和网页，`restrictToWorkspace` 不是 OS 沙箱；不得为尽快通过验收直接把匿名公网消息接到现有 ECS 的完整工具权限。ECS 只读检查显示 `bubblewrap 0.4.0` 与非特权 user namespace 可用、当时内存约 1.3 GiB 可用，仍需设计并实证安全隔离、模型密钥代理和用户数据生命周期。已把原「精灵星球」设想标成历史构想、在[公网设计](../designs/2026-09-24-public-world.design.md)记录纠偏与 Agent 目标，并新增 AC-12；**目前尚未修改/发布线上模型行为或架构**。前一版 8 字符注册修正仍是已发布事实。接续时以当前 Git、线上和用户后续回复为准；先决定安全的 NextClaw Agent 主链路并完成真实验收，再宣称它是 Agent 产品，不以调一条 prompt 或换个名称冒充完成。

补充进度（同日晚间，未发布）：已把模型指令、三组 AI 介绍、首页/对话区/账号文案和星球插画改成直接的共享 AI 表达；`AGENTS.md` 与 README 明确当前 model-only 不算 Agent。40 项测试、TypeScript、lint 已通过，本地 1440px 与 320/390px 真实浏览器检查了版面及宽度，320px 三张卡片底部约为首屏 700px、无横向溢出。一次使用本机供应商配置的真实模型试问在 45 秒后中止，**没有获得模型行为验收**；不能把 prompt 单测或浏览器版面称为实际回复已变自然。上述改动尚未部署；即使部署也只关闭叙事问题，不关闭 AC-12。

后续发布与实测（约 21:07 北京时间）：上述临时纠偏在 `cc89f79` 推送，更新前仅运行 Bibo 专用备份服务并确认 `Result=success/ExecMainStatus=0`；私有 OSS 精确对象 `daily/daily-2026-09-24-210730.5l2kIRsi.tar.gz.gpg` 为 17,297 字节、AES256，未取回解密，不能冒充恢复演练。ECS 后端快进到 `cc89f79`，远端 TypeScript 通过，只重启 Bibo；Worker 版本 `1978b68d-21be-428d-9152-2afeff259cdd`。公网首页、世界 API、原站分别 200/200/200，无边缘密钥回源 403；HTML 新标题及三组新介绍实取可见。本次合成测试用户公网注册成功，向墨里问真实能力，得到真实 DeepSeek Flash 回复，诚实说明能做对话/线索整理、不能读写文件上网或跑代码，报告 2,209 token。该回复仍自发提及旧「精灵/星球」说法且在原 500 输出 token 限额处截断句子，故已继续修改提示词与上限、边缘失败文案，**第二次修正尚未发布和复验**。合成账号及这次公开测试记录留在生产状态，未擅自删除。

## 当前接续检查点（2026-09-24 19:15 北京时间）

用户提醒：会话可能压缩，不能靠模型记忆保存目标和进度。接续时先读本节、下一节的用户目标与未闭合验收，再核对 Git、运行状态和线上实际响应。**不要把本节当作线上实时状态**。产品原意由[共享精灵世界构想](../thoughts/2026-09-22-shared-spirits-world.thought.md)拥有，首版边界由[世界设计](../designs/2026-09-22-first-world.design.md)拥有；本文只记交付目标、证据和下一步。

- **用户的最终要求**：不是原型或只会回一句话的演示，而是公开链接可注册、登录、持续使用真实 AI 的成品；精灵被所有旅人共同遇见，没有默认主人，具有自己的共享经历。用户已授权在必要时使用 GitHub、Cloudflare、阿里云及现有模型配置，要求保护密钥；上线前需从产品吸引力、完整使用链路、可靠性三方面反复审查三次。
- **最近核实的线上事实（19:00，非实时探针）**：公开测试入口为 `https://planet.bibo.bot`；ECS 后端已从 Git `4456d52` 重启运行，后续文档提交不会自动进入正在运行的后端。Cloudflare Worker 仍为 `b4180bb2-6bae-4d38-8419-587948fcfff8`，页面 CSS `index-B3oC7cEM.css`，JS `index-BtBkAKkR.js`。`GET /api/account/deletion-policy` 最近仍为 `enabled:false`，此前删除 POST 实测 503；**公开删除没有启用**。本次发布仍不是正式获客结论。
- **本轮已发布**：按[独立计划](2026-09-24-account-deletion.plan.md)实现并部署账号冻结/额度结转、精灵即时屏蔽与幂等清理、离机记录协调、启动前续做、Cookie 作用域密码复核删除 API，以及「账号与数据」页面。测试覆盖第二只精灵写盘失败、离机写入失败、旧快照 + 离机墓碑重放。30/30 单测、TypeScript、lint、格式、构建和 Worker dry-run 均通过；ECS 上新 unit 通过 `systemd-analyze verify`，并只给原世界目录及独立删除记录目录写权限。**生产原位恢复演练和真实备份保留期仍未完成**，不能把代码已上线写成删除权已交付。
- **发布与真实链路证据**：发布前 Bibo 专用备份服务 `Result=success/ExecMainStatus=0`，私有 OSS 新对象 `daily-2026-09-24-170658.yyZnWJva.tar.gz.gpg` 从 OSS 取回后 SHA-256 `223b86bb515ddbce3e6f82137761670f81874a287f1fd7cda462503a873eb47f` 与 ECS 原件相同，本机私钥流式解密可列出账号和三只精灵状态，下载临时副本已清理。ECS 代码快进、远端 TypeScript 检查后只重启 Bibo；`NRestarts=0`、服务和备份 timer active，下一次自然备份预计 2026-09-25 04:19 北京时间。Worker 发布后，公网匿名世界 API/首页和旧站均 200、无边缘密钥回源 403。新测试旅人经公网 API 注册、向塞拉连续取得两次真实 DeepSeek Flash 回复、本人会话和导出各 4 条、相遇 `4→6`；退出后旧会话为空，重新登录恢复 4 条。此为 **API 路径**，不是当前版本浏览器操作验收。
- **当前版本公网浏览器补验（17:43–17:49）**：桌面浏览器控制仍超时，改用独立 Chrome 开发工具，不改站点代码。已有测试旅人的 320×700 手机视口忽略缓存重载后显示新版「账号与数据」和「模型资料」；从页面向墨里连续发送两条受控问题，得到两条真实 DeepSeek 回复，第二条衔接前一条的银色风筝；相遇 `17→19`、剩余尝试 `10→8`。再次重载后两轮原文和回复仍在，`innerWidth=scrollWidth=320`。打开账号弹窗，宽 284px、无横向溢出；点击导出、接受敏感内容确认后，浏览器实际生成 7,182 字节的 `bibo-planet-personal-data-v1` JSON Blob，含该账号、三只精灵及其墨里 8 条消息，其他两只各 0 条。此证明浏览器生成了完整文件内容，**没有独立核对系统下载目录里的最终文件**。
- **新访客与共享世界补验**：另一个全新隔离 Chrome 身份从公网页面注册合成测试账号、退出并重新登录，登录后只见自己的空白会话和 12 次初始尝试；向同一只墨里问「潮汐门边的银色风筝」，回复提到前一位旅人留下的银色风筝、风声与未知的线索，而该新旅人的页面只显示自己的 2 条消息。该轮 POST `/api/spirits/mori/messages` 返回 200，相遇 `19→20`、新旅人剩余 `12→11`；两个浏览器页面均无 console error/warn/issue。它证明这次受控跨访客影响和私人列表隔离，不保证模型永不复述其他人的原话。
- **本地删除 UI 的先前证据及剩余缺口**：隔离服务 `127.0.0.1:3040` 曾用临时目录、假运营信息和 30 天测试配置打开完整删除确认弹窗，320×700 下可滚动看到密码/确认/按钮；当时浏览器控制在导出确认处挂起。当前公网弹窗准确显示「在线删除尚未开放」。**公网真实删除提交和成功后的 UI 仍不能验收**，直到真实期限、运营信息、恢复与适用边界确认后开启；本地假信息绝不能对外发布。
- **离机基础设施已核实**：ECS 角色获仅 `deletions/` 的列举/读取/写入权，服务身份实际上传、读回及同步初始化标记成功，`daily/` 列举仍为 403；ossutil 2.4.0 校验安装在独立路径，原备份脚本未替换、线上服务未重启。详情与后续生产变量见[运行手册](../OPERATIONS.md)。还未上传任何真实旅人的删除墓碑。
- **适配器实跑补证**：2026-09-24 16:56，北京时间，以 ECS 的 Bibo 服务身份从独立临时克隆运行实际 `OssDeletionRemote`，读取初始化标记成功、合成 UUID 写入再读取成功；管理员精确移除该合成测试对象并复查前缀只剩标记。未上传真实旅人墓碑；当时尚未完成下面的旧快照集成重放。
- **真实离机重放补证**：2026-09-24 16:58，北京时间，同一 ECS 上的隔离临时目录用真实 `OssDeletionRemote` 与两名合成旅人完成三只精灵的旧快照恢复。删除一人后写回旧快照并清空本地墓碑，协调者重新从 OSS 读入墓碑；被删者账号与原文未复活，另一人账号与原文保留。合成测试墓碑在核对后精确清理，OSS 再次只剩格式标记；公网 Bibo API 和旧站各 200。此项关闭的是**隔离环境的 OSS 重放风险**，不是生产原位恢复或真实旅人删除，也不解决备份到期策略。
- **待用户选择**：首发市场（境内公众/海外优先/受邀测试）、可公开的运营者名称和隐私/投诉联系渠道、加密备份保留期。已经非阻塞询问；未答复前不得编造或擅自对外承诺。账号删除代码可以在本地推进，但公开入口和正式获客结论须等真实留存/恢复与相应运营条件齐备。
- **备份范围新增发现**：OSS 生命周期规则不是严格的“N 天内必删”，阿里云按天执行且可能延迟；未启用删除页的文案已改为“按 N 天到期规则清理，实际可能延后”。只读盘点发现本机还有 11 份 `daily-*.gpg`、2 份早期明文 `pre-*.tar.gz` 和 1 份早期加密更新前备份；OSS 除 `daily/` 的 11 份外，还有 `2026-09-24/` 下 2 份早期密文，`deletions/` 只剩格式标记。未来策略必须覆盖这些历史副本而**排除**恢复必需的删除墓碑；尚未删除、迁移或设置到期规则。
- **备份可靠性修复已发布（2026-09-24 17:30 北京时间）**：`cdc5e2a` 修复并发锁被占用时以成功码退出的问题，并在停服务前布置重启保护。ECS 从干净的 `015dc21` 快进到该提交，远端 `bash -n` 通过；持有同一锁再调用脚本，得到 `Another Bibo backup is already running` 和非零结果，Bibo 与 timer 仍 active。随后手动运行原 systemd 备份服务，`Result=success/ExecMainStatus=0`，新密文对象 `daily-2026-09-24-173010.Ag8MOaPm.tar.gz.gpg` 在本地和私有 OSS 同为 14,075 字节，OSS ETag 与本地 MD5 均为 `719cf6efc3f48096997cd8b24ed82072`；服务 active、`NRestarts=0`，公网 Bibo 首页/世界 API 和旧站各 200。此项证明手动正常路径与锁失败路径，不冒充明天首次自然 timer 触发、生产原位恢复或备份保留期配置。
- **AC-05 预算口径纠正（待增强）**：`240/天` 是当前单实例的**业务层唤醒尝试**上限，并非供应商 HTTP 请求数硬上限。检查本项目安装的 `@nextclaw/core@0.18.3` 发现 OpenAI 兼容适配器在瞬时错误时每个 API base 最多重试 3 次，根路径还可能回退到 `/v1`；一次已预留的唤醒可能产生多次供应商请求。17:39 使用生产环境已有密钥只读调用[DeepSeek 余额接口](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)，`is_available=true`、币种为 CNY；未输出密钥或具体余额，也未充值。这只证明当时接口可用，不等于有低余额告警或账单硬限制。实际用量/余额监测与成本保护仍需闭合，不能以现有次数测试宣布 AC-05 完全通过；[计划中的错误口径](2026-09-24-model-attempt-budget.plan.md)和[运行手册](../OPERATIONS.md)已同步修正。
- **下一动作**：当前版本注册、连续对话、跨访客共享、本人数据导出的浏览器路径已补证；仍需在真实期限与公开运营信息确认后配置覆盖历史副本的备份清理，才能开放并实测公网删除。观察首次自然定时备份，完成 AC-05 实际费用监控及 AC-10/AC-11 的运营/法规边界，最后复核三轮审查的全部 Required 项。任何局部通过都不能代替全链路验收。
- **本次上下文接续与工作区归属**：用户再次要求把长期目标和进度落盘，避免会话压缩造成遗忘。本段是接续锚点，仍须用线上实况复验。本轮开始时本地 `master` 为已推送的 `629aaa6`、工作区干净；本任务只改 `src/server/auth-store.ts`、`test/auth-store.test.ts`、本计划及公网设计文档。发现 30 天会话到期后虽在读取时拒绝，但 `accounts.json` 中对应 token 哈希仍留存；现已加启动时只移除过期会话哈希的代码及“过期+有效并存”回归测试，设计见[过期会话哈希的启动清理](../designs/2026-09-24-public-world.design.md#过期会话哈希的启动清理)。先写失败测试 8 过/1 失败，修改后定向测试 9/9 通过；全量 TypeScript、31/31 测试、lint、格式、构建及 `git diff --check` 均通过。跨仓库维护性脚本未识别 Bibo 的变更，不能将“not applicable”冒充自动代码 Review 通过；人工逐行复核见下一条。该清理不等于账号/对话删除，也不解决加密备份保留期。
- **18:04 代码与生产只读复核**：人工逐行核对本次代码 diff：仅在 `AuthStore.initialize()` 读取已有状态后按到期时间过滤会话，确有变化才走原有原子 `persist`，失败会阻止启动；现有有效会话不变，31 项测试含过期/有效并存回归。未见本次 diff 的开放 finding；跨仓库自动维护性脚本仍不适用，故不写成自动 Review 通过。发布前 ECS Git 为干净的 `cdc5e2a`，Bibo 和备份 timer 均 active、`NRestarts=0`、最近一次备份 service 为 success；只输出聚合计数：14 个账号、17 条会话、当时已过期 0 条。故本次生产重启最多能证明兼容旧状态，**不能**实测线上过期项被清走；该行为由隔离持久化回归测试证明。
- **18:07 发布结果**：修复与上述两份文档在 `91152b0` 提交并推送 GitHub。更新前运行生产原有 Bibo 备份 service，`Result=success/ExecMainStatus=0`，Bibo 与 timer 均恢复 active；私有 OSS 独立列举到新加密对象 `daily-2026-09-24-180541.haMhEKFD.tar.gz.gpg`（15,379 字节）。ECS 从干净的 `cdc5e2a` 快进到 `91152b0`，远端 TypeScript 检查通过，仅重启 Bibo，服务 active、`NRestarts=0`，远端 Git 干净。公网首页、世界 API、删除策略及同机旧站均 200；原有隔离 Chrome 身份的 `/api/session` 重启后仍返回已登录，剩余 11 次，世界仍有三只精灵，删除开关仍为 `false`。**本轮没有自然过期的线上会话，也未重新下载/解密这份最新备份**；不能据此宣称生产实际清理过期项或最新备份完成恢复演练。AC-05、AC-06 的首次自然 timer、AC-10/AC-11 及最终 AC-08 仍开放。
- **18:12 余额风险与跟进**：使用生产已有凭据仅调用 DeepSeek 官方[余额只读接口](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)，工具只输出 `is_available=true`、币种 CNY 和 **1–10 元**区间，不输出密钥或精确余额。该钱包由供应商账号共享，无法从余额变化归因 Bibo 的实际费用；DeepSeek 官方[价格表](https://api-docs.deepseek.com/quick_start/pricing/)按输入/输出 token 及峰谷计费，也可能变动。已更新既有每日 Bibo 跟进，要求检查余额可用性/异常下降与首次自然备份，异常时通知，不把监测冒充金额硬上限。余额偏低已向用户报告，并非阻塞询问 Bibo 的月度模型预算及是否授权在上限内充值；收到答复前不充值。AC-05 仍缺 Bibo 独立实际费用观测或有效账单约束，不能因为钱包尚可用、每日 240 次唤醒限制或自动跟进就标为通过。
- **18:16 Bibo 自身用量的只读聚合**：不输出账号、消息或密钥，直接读取 ECS 上账号与三只精灵的生产状态；当日账号尝试计数（含额度结转）34，世界成功相遇 34，成功记录中的模型报告 token 合计 31,946，估算 token 记账为 0。Bibo 与备份 timer active。按 DeepSeek 当时公开的 Flash 峰时最高输出单价每百万 token 1.2 美元，即使把这些**已成功回复的全部报告 token** 都按最高输出价计算，理论上也不足 0.04 美元；这是便于理解数量级的上界计算，**不包括**供应商内部重试、未记录的失败请求、共享账户其他调用或未来价格变化，不能作为真实 Bibo 账单或未来 240 次的费用上限。34 次尝试与 34 次相遇相等只说明业务层记录在此时一致，不证明供应商只收到 34 个 HTTP 请求。AC-05 仍开放。
- **18:18 最新离机备份隔离恢复**：精确取回私有 OSS `daily-2026-09-24-180541.haMhEKFD.tar.gz.gpg`，15,379 字节，密文 SHA-256 `0c2e943bb82a5198d615d880445e367027251ed7d8f19e36503157cfda9d25df` 与 ECS 原文件一致。用本机受限私钥在权限受限临时目录解密，归档含账号文件及三只精灵状态；当前 `AuthStore.initialize(true)` 和 `WorldStore.initialize(true)` 加载成功，聚合出 14 个账号、17 条会话、墨里/皮可/塞拉相遇 20/8/6，按本人数据读取共 68 条消息和 34 次本人遭遇。只输出聚合量，不输出原文、身份或凭据；下载密文和临时明文已从精确目录删除并复查不存在。**这比此前旧备份恢复更贴近当前数据，但仍不是生产原位覆盖恢复，也未观察到首次自然 timer 触发。**
- **18:27 陌生访客手机首屏纠偏**：新的隔离匿名公网 Chrome 视口发现，原版 320×700 首屏的三只卡片位于约 620–735px，只露出顶部；整页截图无横向溢出曾掩盖这个首次使用缺口。保留“先看世界再遇见精灵”顺序，短屏只收紧星球舞台；设计候选与边界见[公网设计](../designs/2026-09-24-public-world.design.md#极窄手机上的同一信号)。`076fb48` 已推送，TypeScript、31/31 测试、lint、格式、构建、Worker dry-run 与 diff 检查通过，纯前端发布 Worker `b4180bb2-6bae-4d38-8419-587948fcfff8`，未重启 ECS。公网页面忽略缓存重载并**只截初始视口**：320×700 三卡底部约 690px，375×667 约 658px，三只名字和活动提示均可见，`scrollWidth=innerWidth`；375px 真实点击皮可后对话区顶部约 0px，能继续操作。390×844 与 1440×900 视觉复查无退化、桌面输入框仍在首屏；公网页面/世界/会话 API 与同机原站均 200。此项修复增强首次可发现性，但真实陌生用户的吸引力仍需行为数据，不因 AI 审查宣布获客效果已证明。
- **18:42 注册/登录资源保护已发布**：复核发现原实现只按 IP 限速，成功注册没有全站持久上限，尝试次数 Map 可随不同来源无界增长。按[公网设计](../designs/2026-09-24-public-world.design.md#资源与滥用边界)在 `AuthStore` 加每天最多 240 次成功注册的持久计数、全站短窗注册 1200 次/10 分钟和登录 600 次/10 分钟、最多 10,000 个 IP 尝试键；保留既有单 IP 限额。全量 36/36 测试、TypeScript、lint、格式、构建、diff 检查通过，人工核对改动不触及模型或删除链路；Git `56ef432` 已推送。发布前专用备份 service success，私有 OSS 独立列举到新加密对象 `daily-2026-09-24-183933.CQjARPYj.tar.gz.gpg`（15,379 字节）；**本次没有重新取回解密它**，18:18 的前一份备份已有隔离恢复证据。ECS 从干净的 `91152b0` 快进到 `56ef432`，远端 TypeScript 检查通过，只重启 Bibo，服务和 timer active、`NRestarts=0`、远端 Git 干净。公网首页/世界/匿名会话与同机旧站均 200，无边缘密钥回源为 403。一个新增合成测试旅人经公网 API 注册→保持会话→退出→重新登录→恢复同一账号，各步 200；服务器账号文件仅聚合检查到 15 个账号、当天注册计数 15。没有为了验证上限在生产制造 240 个账号，429 限额与 Map 上界由本地回归测试证明；此保护**不是**大型 DDoS 防护、独立账单硬上限或正式获客资格。
- **18:47 供应商余额与跟进频率**：用 ECS 上现有凭据只读调用 DeepSeek 官方[余额接口](https://api-docs.deepseek.com/api/get-user-balance/)，仅输出可用性、币种和区间：`is_available=true`，CNY，余额仍在 **1–10 元**，不输出精确余额或密钥。官方[限速文档](https://api-docs.deepseek.com/quick_start/rate_limit/)明确并发额度按供应商账号而非 API Key 计算；当前材料不能据此证明新建 Key 能隔离账单或设置 Bibo 专属消费硬上限。把既有 Bibo 跟进从每天 09:00 调整为每天 01:00/07:00/13:00/19:00，只读关注余额不可用、低于 1 元或异常下降，也更早复核首次自然备份；已知的 1–10 元状态不重复通知。**这只是更及时的告警，不是自动充值、Bibo 独立费用核算或金额硬保护。**月度模型预算与充值权限仍待用户决定；AC-05 继续部分通过。
- **19:00 会话存储可靠性修复已发布**：审查发现过期会话哈希原来只在启动时清理；反复成功登录会追加有效哈希，伪造格式正确 Cookie 的无效退出也会重写账号文件。按[会话生命周期设计](../designs/2026-09-24-public-world.design.md#持续运行中的会话数量与无效退出)在 `AuthStore` 的单一串行写入中实现：成功注册/登录顺带清除过期哈希，每账号最多 8 个有效会话，第 9 个仅淘汰该账号最早的会话；未知或重复退出无写盘、HTTP 响应不暴露会话是否存在。修前 3 条回归失败，修后定向与全量 39/39 测试、TypeScript、lint、格式、构建、diff 检查通过；组装 HTTP 测试覆盖伪造/真实退出和另一个账号不受影响，冷重载测试覆盖第 9 个会话淘汰。跨仓库 maintainability 脚本对 Bibo 仍返回 not applicable，人工 diff Review 无开放 finding；世界状态长期增长未被本改动解决。Git `4456d52` 已推送，发布前 Bibo 专用备份 service success，私有 OSS 独立列举到 `daily-2026-09-24-185726.CzXLrd0u.tar.gz.gpg`（15,599 字节）；**本次新对象未重新取回解密**。ECS 从干净的 `56ef432` 快进，远端 TypeScript 通过后仅重启 Bibo，服务/timer active、`NRestarts=0`、远端 Git 干净。原有隔离 Chrome 测试旅人的 `/api/session` 重启后仍为已登录；新合成旅人从公网 API 完成注册→伪造退出不影响本人→真实退出旧会话失效→重新登录恢复同一账号，全部 HTTP 200 且状态一致。公网首页/世界与同机旧站均 200，无密钥回源 403。**未在生产制造 9 个会话、未做本轮真实模型调用；上限/淘汰由隔离持久化测试证明，既有模型链路证据属前一版本。**正式获客门槛仍开放。
- **19:01 自然备份定时器的当前等待状态**：ECS `bibo-planet-backup.timer` 为 `active`，`NextElapseUSecRealtime=2026-09-25 04:15:22 CST`，`LastTriggerUSec` 仍空；最近手动备份 service 是 success。此为经 systemd 确认的等待中任务，**不是**自然触发成功。既有 07:00 跟进会在预计触发后核对 service、私有 OSS 精确新对象及可恢复性；若发生失败需按运行手册恢复，不能以手动成功替代 AC-06。
- **19:04 当前后端的真实模型浏览器补验**：在已登录的隔离公网 Chrome 旅人页面，沿正常 UI 向墨里追问此前的「潮汐门边银色风筝」，得到延续同一线索的真实 DeepSeek Flash 回复，非预制或测试模型；墨里相遇 `20→21`、能量 `227,892→225,637`（报告消耗 2,255 token）、该旅人剩余尝试 `11→10`。忽略缓存刷新后本人提问、模型回复、相遇和额度仍在，浏览器无 error/warn/issue。此为 ECS `4456d52` + 既有 Worker 的真实单轮继续对话与持久化证据；三只精灵连续两轮、两旅人共享和故障分支沿用此前版本与隔离测试证据，**不**把这一轮扩大成三轮审查全通过或正式获客结论。因 DeepSeek 余额仍处 1–10 元区间，本次只做必要的一轮，没有进一步消耗模型额度。
- **19:08 模型费用的可审计量级，而非承诺上限**：核对 DeepSeek 当前[中文官方价格与扣费规则](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)：Flash 高峰时输入缓存未命中 2 元/百万 token、输出 8 元/百万 token，费用从赠送或充值余额扣减；[402 文档](https://api-docs.deepseek.com/zh-cn/quick_start/error_codes/)将余额不足列为不能继续调用。对 18:16 已记录的 34 次**成功回复**、合计 31,946 个报告 token，即使全部按较贵的输出价计算，其对应 token 的名义费用也不超过 `31,946 × 8 / 1,000,000 = 0.256 元`；这是这批成功记录的粗上界，不是供应商账单。若机械外推当时每次约 940 token 到 240 次/天，则同一极端单价约 1.80 元/天，但样本是合成测试，输入会随共同经历增长；失败调用、适配器重试、共享钱包其他项目和以后调价均未计入。现有预付余额机制可能限制供应商继续提供服务，**不等于 Bibo 独立月度预算、自动充值状态或不中断服务已受控**。没有用户给出的预算/充值决策前，不作充值，不将 AC-05 标为通过。
- **19:10 有限能量的持续可用性风险**：公网匿名 `/api/world` 只读显示墨里/皮可/塞拉当前能量 `225,637 / 243,675 / 246,487`，相遇 `21 / 8 / 6`；初始各 250,000，现合计剩余 `715,799`。按这 35 次历史相遇平均约 `977` 点/次机械外推，全部精灵合计约还能完成 730 次类似对话；若未来满载 240 次成功唤醒/天，量级约 3 天，若流量集中在墨里则可能更早。实际耗能会受消息、共享记忆和模型用量影响，不是保证次数。当前只有管理员停服务运行 `energy:add` 的补能路径，尚无自动恢复、访客贡献或预告机制；这符合“能量是真约束”，却让公开获客后的持续可用性依赖人工操作。已向用户单独询问首版耗尽策略，在得到产品选择和费用预算前不把有限世界擅自改成无限/每日恢复；此项纳入 AC-05/AC-08 的正式交付审查。
- **19:11 生产状态容量与定时器只读复核**：通过阿里云 ECS Cloud Assistant 在已记录的 Bibo 实例上只运行 `du -b`、`df -B1` 与 `systemctl show`；`accounts.json` 9,021 字节，三只精灵状态分别 32,141 / 13,113 / 7,290 字节，四份状态合计 61,565 字节；根分区可用 32,556,138,496 字节，使用率 19%。现阶段没有磁盘空间压力，**不据此推断 JSON 全量重写和长期召回在大规模历史下仍性能足够**；先处理有限能量及费用策略，不为当前 62 KB 状态贸然迁移存储。备份 timer `ActiveState=active`、下一次 `2026-09-25 04:15:22 CST`、`LastTriggerUSec` 仍空；这是实时确认正在等待的定时器，不是首次自然备份已经成功。
- **19:15 用量归因调查与全新访客复看**：当前安装的 `@nextclaw/core@0.18.3` 的 `LLMResponse.usage` 是数值记录，Chat Completions 适配器保留并归一化供应商返回的输入、输出和可能的缓存 token 字段；Bibo 目前只从中读取总 token 并在成功遭遇中持久保存。补记细分字段可以改善**成功且写盘的调用**的 Bibo 自身估算，却仍漏掉供应商内部重试、失败计费或“模型已回应但本地写盘失败”，不能作为 Bibo 独立真实账单或金额硬上限；尚未改动代码，待预算/归因口径决策后设计。独立匿名 Chrome context 打开当前公网页面，首屏无障碍树与截图均清楚显示“精灵不属于任何人”、三只精灵、最近相遇信号及对话入口；这是一次视觉/信息架构复核，不是新用户转化数据，也未再消耗模型 token。

## 本轮接续状态（2026-09-24 16:00 北京时间）

用户再次明确要求：不要只依赖会被压缩的会话上下文；把原始诉求、当前目标、已经核实的结果、进行中的改动、阻塞与下一步持续写在本文件，方便用户随时查看。本文是 **Bibo Planet 交付进度的单一执行锚点**；产品构想、架构事实和运维步骤分别由下方链接的 thought、design、运行手册拥有，不在个人知识库复制项目专属进度。每轮恢复先读本节及「用户目标与不可缩水的验收合同」，再检查 Git 和线上实际状态；不要把文档当作实时服务探针。

- **目标不变**：最终交付可公开访问、可注册登录、与真实共享 AI 精灵持续对话的产品，并完成产品吸引力、完整用户链路、上线可靠性三方面各三轮审查。`https://planet.bibo.bot` 当前是运行中的公开测试，**不是**已通过所有正式获客门槛的成品。
- **已发布基线**：后端与前端/Worker 源均为 Git `f402a00`；Cloudflare Worker `6e704e07-d179-4133-820c-15eda0dedf4d`，网页静态 JS `index-msSQyIEg.js`；Bibo 生产配置为 `deepseek/deepseek-flash`。发布前代码区干净、四份状态文件非空，Bibo 专用备份服务 success，私有 OSS 独立查到加密对象 `daily-2026-09-24-154626.orIJXJ7s.tar.gz.gpg`；此精确对象随后已取回并在隔离目录恢复验证，见下文。只重启了 Bibo，服务与备份 timer active，同机 NextClaw 旧站 200。上一个版本 `24e74dc` 和对应 Worker 版本是历史发布锚点，不是当前运行版本。
- **模型切换的理由与披露边界**：DeepSeek 2026-04-24 的[官方更新记录](https://api-docs.deepseek.com/updates/)曾宣布旧 `deepseek-chat` alias 将于 2026-07-24 停用，虽然切换前生产调用仍成功；2026-09-10 的更新推荐以 `deepseek-flash` 调用最新 Flash。新 Flash 的特定备案号尚未核实，当前 `/api/world` 只返回 `DeepSeek Flash`、`filingNumber: null` 和[模型资料](https://api-docs.deepseek.com/quick_start/pricing/)链接，不挪用旧 [Deepseek Chat 公示号](https://cdn.deepseek.com/policies/zh-CN/model-algorithm-disclosure.html)。这不表示应用自身的 AI 服务核定已完成。
- **新模型已验证范围**：本机隔离临时世界经真实 NextClaw Harness + DeepSeek Flash 成功调用，两个访客的共享线索可被后者提及；ECS 目标环境用现有生产凭据和独立临时目录实调成功，模型报告 312 token，临时目录已清理。发布前 TypeScript、20/20 测试、lint、格式、构建、Worker dry-run 通过；远端代码快进后 TypeScript 通过。公网 API 用两名新注册测试旅人向墨里完成 A 两轮、B 一轮：A 第二轮接续「银色风筝」，B 从同一精灵回应得知这条线索；A 私人历史 4 条、B 2 条，相遇 `14→17`，各自当日剩余额度 10/11。公网首页、世界/会话 API 与原站均 200，无密钥源站 Bibo 路径 403；首页引用新 JS，该资产包含「模型资料 ↗」文案。这是**公网真实 API**证据，不是更新后浏览器交互验收。
- **浏览器复验状态**：本轮统一浏览器控制连续超时，未取得当前版本的页面截图或实际点击/输入证据；此前 320px、桌面和连续对话的浏览器证据属于旧前端发布时的页面。后续工具恢复后，需复验模型资料链接、注册与两轮连续发送、新额度文案和 320px 页面。不能因 API 和静态资源均成功就写成浏览器通过。
- **AC-05 模型调用次数保护已发布**：按[模型尝试预算计划](2026-09-24-model-attempt-budget.plan.md)，`AuthStore` 在模型调用前持久预留；个人每天 18 次尝试 / 12 次成功，全星球每天 240 次尝试，失败不记成功但占一次尝试。本机 24/24 测试涵盖失败、旧账号、重启、同账号与跨账号并发、全局第 241 次拒绝，TypeScript、lint、格式、构建和 Worker dry-run 通过。远端用新代码只读加载既有账号成功，11 条未带新字段的历史账号行可按同日成功次数兼容读取。公网新旅人向皮可获得真实 Flash 回复：本人档案 `attemptCount=1`、`usageCount=1`、私人会话 2 条、皮可相遇 `7→8`；相同 `requestId` 重放标为 replayed，尝试/成功仍各 1、相遇不再增加。**这证明当前单实例路径有每日调用次数上限，不证明供应商账单金额绝对上限或未来多实例共享预算。**
- **最新备份的隔离恢复**：从私有 OSS 取回精确对象 `daily-2026-09-24-154626.orIJXJ7s.tar.gz.gpg`，下载密文 SHA-256 `38053ad385f50e19333b1de02ee13b229f74a3163d0fc2ede4247219e163b141` 与 ECS 原文件一致；本机受限临时目录解密后用当前 `AuthStore`、`WorldStore` 加载出 11 个账号、3 只精灵，相遇数墨里/皮可/塞拉分别 17/7/4，共 56 条私人消息。只输出聚合计数，不输出身份或对话原文；临时明文和下载副本已自动清理，之后查不到残留临时目录。证明该精确离机对象可读，**仍不是生产原位恢复或自然 timer 触发**。
- **网络观察**：本机配置了代理。针对同一公网世界 API 的五次小样本：经默认代理的 HTTP/2 为 4/5 次 200、HTTP/1.1 为 4/5 次 200，失败均是本机 TLS 握手错误；绕过代理直连为 5/5 次 200，但耗时约 1.63–6.21 秒。从杭州 ECS 访问同一公网 API 5/5 次 200、约 1.10–5.48 秒。此前也观察到 HTTP/2 framing 错误。**推断**本机代理路径可能参与了部分失败，但样本很小，不足以排除 Cloudflare 或地区链路问题，也不能证明国内真实用户体验达标。原站仍 200、无密钥源站路径 403；首发市场选择会影响后续入口优化。
- **下一执行序列**：浏览器工具恢复后完成当前版本 UI 复验；观察 AC-06 首次自然备份，处理 AC-10/AC-11 的真实运营与法规边界，并继续评估国内访问延迟，最后做可靠性审查。若新模型生产链路出现故障，保留世界数据、先核对模型供应商状态，再按运行手册回滚配置/代码，不伪造回复。
- **仍未闭合的验收**：AC-06 首次自然定时备份（预计 2026-09-25 04:17:51 北京时间）；AC-10 运营者身份/联系渠道、用户删除与备份期限、域名/接入适用性；AC-11 中国境内公众 AI 与拟人化服务适用性及新模型披露；AC-08 当前版本浏览器复验、最后一轮可靠性和整体交付审查。已向用户询问首发市场、公开运营者联系方式、备份保留期，尚无答复；不把猜测写成已决策。

## 此前工作快照（2026-09-24 15:13 北京时间）

> 当前 ECS 后端仍为 Git `bef2fd8`；前端/Worker 源码为 Git `53fc153`，Cloudflare Worker `510cf954-4f8f-4dad-b4d2-34146f184d8a`。本次只发布前端资产与同一 Worker 代码，没有改后端 API 或重启 ECS 服务。以下旧 SHA 段落是历史证据，不是当前前端或后端版本；文档后续提交不改变线上二进制。

- **共享记忆告知已发布**：`53fc153` 把匿名首页与注册/登录弹窗的提示统一为「发送成功的内容会成为精灵的共同记忆；其他旅人可能从它的回应中得知。请勿输入隐私或秘密。」提示字号升至 12px，弹窗文字在浅背景上的颜色对比度计算约为 5.93:1。20/20 测试、TypeScript、lint、格式、构建与 Worker dry-run 通过；本地真实 Chrome 的 320×700 手机弹窗完整显示，`scrollWidth=innerWidth=320`、告知底部位于视口内；1440×900 桌面首屏仍完整，页面高度 900、发送区底部 844。公网 Worker 发布后，新匿名 Chrome 页面与 320px 手机注册弹窗均实际显示新告知，弹窗宽度无横向溢出；公网首页/世界/会话 API 和旧站均为 200，无边缘密钥源站路径 403。这只补产品事实告知，**不是**完整隐私政策、同意记录或 AC-10/AC-11 通过。

- **首屏性能的地域差异**：2026-09-24 15:00–15:03（北京时间）用新的匿名 Chrome 身份测公网首页，浏览器访问 Cloudflare `NRT`、来源标记 `JP`：无节流桌面导航 LCP 370 ms、CLS 0.01；390×844 移动仿真加 Slow 4G/4 倍 CPU 降速时 LCP 1445 ms、CLS 0.02，世界与会话 API 各约 0.58 s。未发现可带来首屏指标节省的渲染阻塞、缓存或旧 JS 优化，暂不为这组实验改前端。**该日本路径不能代表国内访客。** 同一时段从此前 Cloudflare trace 标记为 `loc=CN, colo=AMS` 的本机直连公网 `/api/world`，5 个并发新连接均为 200，TTFB 分别约 2.17/2.35/1.97/4.28/1.96 s；这是小样本 CLI 新连接，并非浏览器连接复用或真实用户分布，但国内路径长尾仍值得关注。是否调整入口、接入与后端区域，依赖首发市场选择和接入合规核定，不能凭日本浏览器快照宣称国内体验达标。

- **跨旅人原话索取探测**：2026-09-24 14:55–14:57（北京时间），另一个全新隔离 Chrome 旅人向同一只墨里连续两轮索取前一个受控测试旅人的提问原文，一次泛问、一次带「潮汐门」话题线索。公网真实 DeepSeek 两次都拒绝逐字转交，同时保留共同世界的线索回应；该旅人的剩余额度 `12→11→10`，墨里相遇 `12→13→14`。这只是两条提示下的观察，**不能据此证明模型对所有越权提示都不会复述原文**；原始遭遇文本仍作为共享记忆进入模型上下文。页面已提示不要留下秘密；正式隐私告知仍须如实解释这一机制，不能只拿 API 历史列表隔离来宣称绝对保密。

- **新版公网浏览器连续对话已复验**：2026-09-24 14:47–14:50（北京时间），全新隔离 Chrome 身份打开 `https://planet.bibo.bot`，从实际页面注册旅人，向墨里连续发送两句话；两次均在页面显示真实 DeepSeek 回复，第二次明确衔接第一轮的「潮汐门」，不是占位或模拟文本。墨里相遇 `10→11→12`、能量 `243943→242891→241700`，该旅人当日剩余 `12→11→10`；输入框在成功后清空。忽略缓存刷新后，同一登录身份仍看到完整四条消息、相遇 12 和剩余 10。再用 Chrome 移动设备仿真设为 `320×700`，`innerWidth=scrollWidth=320`，两条 AI 回复仍在页面。这补齐了 `b095bc9` 此前缺失的**新版公网浏览器**证据，直接覆盖「发一句话就不能继续」的回归路径；只证明此次实测账号/时段，不代表所有供应商故障或所有地区网络都已通过。

- **首发市场决策待用户选择**：依据已施行的[拟人化互动服务办法](https://www.cac.gov.cn/2026-04/10/c_1777558395078289.htm)第二、二十二、二十六条，若 Bibo 被认定为向境内公众提供持续性情感互动服务，则“上线”本身是安全评估触发项，百万注册/十万月活是另一个独立触发项，另有算法备案；“精灵/游戏/公开测试”的叫法不自动排除适用。Bibo 是否落入该定义仍待实际服务的专业或主管核定；即使不落入，面向境内公众生成文本仍须评估[生成式 AI 办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)的独立义务。具体逐条差距和境内优先/海外先行两条路径见[首发市场决策门](../designs/2026-09-24-public-world.design.md#首发市场与拟人化互动适用性的决策门)。已非阻塞向用户询问首发市场；答复前不擅自迁移、限流地区或宣传完成正式境内获客条件。

- **生产缺失数据保护已发布**：`bef2fd8` 让生产进程启动时要求账号和三只精灵状态都已存在，发现缺失立即失败，不再自动创建“空白新星球”；本地首次开发启动仍可初始化全新世界，部分精灵状态丢失时同样拒绝悄悄重建。20/20 测试、TypeScript、lint、格式、构建通过；隔离空目录运行真实 `main.ts` 证实生产模式退出且没有写出新状态。ECS 更新前只读确认四份状态文件非空、`NODE_ENV=production`、Bibo 与备份 timer active；仅 Bibo 专用备份服务 success，私有 OSS 确认新加密对象 `daily-2026-09-24-143853.gKch4XDF.tar.gz.gpg` 存在。只重启 Bibo 后三只精灵的能量和相遇数未回退，公网首页/世界 API、原站均 200，无边缘密钥源站路径 403。公网新旅人实际注册、会话保持并与塞拉获得真实模型回复，本人历史 2 条、塞拉相遇 `3→4`。这证明现有数据上可启动，不等于已做生产数据丢失后的恢复演练；首次自然 timer 和 AC-10/AC-11/最终 AC-08 仍开放。

- **最新消息可靠性修复已发布**：`b095bc9` 让成功的 POST 直接返回精灵持久化状态，前端立即显示真实回复，不再依赖第二次 GET 才确认成功；延迟的旧会话读取不会覆盖新回复，幂等重放也不会把相遇数虚加一。18/18 测试、TypeScript、lint、格式、构建、Worker dry-run 通过。隔离本地浏览器用测试模型实走注册→发送→回复立即出现→刷新后历史与计数一致；公网新旅人经真实 DeepSeek 连续两轮，世界相遇数 `8→10`、本人会话 4 条、额度 `12→10`，重放首轮请求不再增加相遇或扣额。公网浏览器自动控制本次超时，故此处分别报告本地 UI 与公网 API 证据，不冒充完整公网浏览器验收。发布前专用备份 service success、私有 OSS 对象 `daily-2026-09-24-142648.RoNUEwOr.tar.gz.gpg` 已独立确认；只重启 Bibo，服务与备份 timer active。公网首页、世界接口、原站均 200，无密钥源站 Bibo 路径 403；Cloudflare Worker 版本如上。自然 timer、AC-10/AC-11、最终 AC-08 仍未闭合。

- **此前数据导出阶段**：已将旧「导出对话」补成登录旅人的「导出我的数据」。档案包含账号基本资料、三只精灵各自的本人会话，以及本人贡献的共同遭遇；不包含密码、会话哈希或其他旅人的原始会话/遭遇。精灵的回复可能因共享记忆提及他人的线索，这不等于导出他人的原始记录。此工作与账号删除、备份期限、运营者联系渠道和备案/AI 服务要求是不同事项，不能借导出完成来宣称 AC-10/AC-11 通过。
- **此前数据导出发布状态**：`61a25ca` 已提交并推送至独立 GitHub 仓库，ECS 后端同 SHA、Cloudflare Worker 版本 `e3d4f6e2-18fa-4c21-8f2e-ab6a280b09c8` 同源发布。最终检查为 16/16 测试、TypeScript、lint、构建、格式、Worker dry-run 和 `git diff --check` 全通过。发布前 ECS 干净、Bibo 与备份 timer active、Nginx 配置有效、原站 200；专用备份服务执行 success，加密对象 `daily-2026-09-24-132521.3Byfnthr.tar.gz.gpg` 已由独立 OSS 列表确认存在。随后从 OSS 下载该精确对象，SHA-256 `ef6ce100821ab738951e75f899b5f4786034950026164f5b67085166a668fd55` 与 ECS 文件一致；本机私钥流式解密和 `tar` 完整读取到 8 个条目，临时加密副本已清理，未落盘明文。此证明离机对象可读取，但没有做生产原位恢复，也不代替首个自然定时触发观察。
- **浏览器与公网证据**：隔离本地浏览器曾用测试模型走通注册、对话、下载与取消，临时服务已关闭；这只证明 UI/传输。发布后两个已有的独立公网 Chrome 身份从页面实际下载新格式 JSON：A 为墨里 8 条本人会话/4 条本人遭遇、其他精灵 0；B 为墨里 2/1、皮可 4/2、塞拉 0。账号 ID 不同，双方导出的遭遇原文只包含本人发送的线索，不出现对方的原始提问；账号字段仅 `id/name/createdAt/usageDay/usageCount`，没有密码哈希或会话字段。匿名 `/api/account/data` 为 401、无导出按钮；320px `scrollWidth=innerWidth=320`。公网首页与世界 API、原 `nextclaw.net` 主页均为 200；没有边缘密钥的源站 Bibo 路径为 403。随后在 320px 真实公网浏览器里，已有账号向皮可追问其先前发明的「追风签」如何让多人一起玩，模型延续上下文给出接力玩法，页面显示模型报告消耗 1304 token，皮可能量 `246563→245259`、相遇 `6→7`、该账号当日剩余 `9→8`；证明此次后端更新后的真实模型链路仍可用，但不代表真实访客愿意持续回来。
- **仍开放的交付门槛**：首个自然定时备份（预计 2026-09-25 04:15 北京时间）、AC-10 数据权利/运营者/域名、AC-11 境内 AI 服务适用要求、AC-08 最终可靠性审查。它们未闭合前，`https://planet.bibo.bot` 只能称为运行中的公开测试产品，不称为可正式获客的完整成品。
- **此前模型公示发布状态**：`9046ef1` 已推送并部署，ECS 后端同 SHA，Cloudflare Worker 版本 `85bc613c-115c-45e8-aa30-c2ef000c21d6`。`/api/world` 按当前 `deepseek/deepseek-chat` 返回 DeepSeek 官方公示的 `Deepseek Chat / Beijing-DeepseekChat-202404280016` 和来源链接；对话标题旁直接显示，未核验模型不会沿用旧号。17 项测试、TypeScript、lint、构建、格式与 Worker dry-run 通过；隔离本地浏览器和正式公网的 1440×900、320×700 页面均已检查：无横向溢出，桌面首屏完整，手机公示文字可读。生产配置只读核对为 `deepseek/deepseek-chat`。发布前专用备份 service success、加密对象 `daily-2026-09-24-135753.xy6UmX2X.tar.gz.gpg` 已在私有 OSS 列表确认存在；只重启 Bibo，同机旧站正常。模型自身的备案号**不是** Bibo 应用的登记/备案，AC-10/AC-11 仍未通过。
- **最新离机备份的隔离恢复**：2026-09-24 从私有 OSS 取回上述 `13:57` 加密对象，SHA-256 `edc9a83dcd1893efe54bcae50691649bb06e0a63dd3c5a8fe4744e83d3bbda0c` 与 ECS 原文件一致；本机私钥解密到权限受限的临时目录后，用当前 `AuthStore` 和 `WorldStore` 完整加载：5 个账号、3 只精灵、遭遇数分别为 8/7/3，共 36 条私人消息。只输出聚合计数，未输出姓名、密码、会话或对话正文；临时目录在同一验证进程的 `finally` 中清理。此项证明该精确离机对象能被当前代码隔离读取，仍不等于生产原位覆盖恢复，也不等于首次自然 timer 触发。

## 接续入口（每次恢复上下文先读）

1. 先读下方「用户目标与不可缩水的验收合同」和 [上下文压缩后的恢复锚点](#上下文压缩后的恢复锚点)，再读「待决策与执行序列」；不要把历史进展段落误认为当前状态。
2. 产品为何存在、哪些开放性不能被实现方案抹掉，见[无主 AI 星球的产品构想](../thoughts/2026-09-22-shared-spirits-world.thought.md)；现行公网架构和安全取舍见[公网设计](../designs/2026-09-24-public-world.design.md)；部署、备份和回滚见[运行手册](../OPERATIONS.md)。
3. 截至 2026-09-24，`https://planet.bibo.bot` 已可注册并与真实共享精灵对话，但**尚不能宣称是完成合规核定、可以直接面向国内访客正式获客的成品**。自然定时备份尚待首次触发；运营者公开身份与联系渠道、隐私/删除及备份期限、`.bot` 域名的内地备案可行性、境内公众 AI 服务要求仍待闭合。三轮产品和交互审查已有证据，最终可靠性与整体交付结论仍为未通过。
4. 后续每次重要发现或修复，更新本文件对应 AC 的状态和带时间的证据，必要时同步设计或运行手册；完成前继续追踪，不以一次会话结束、上下文压缩或网站返回 200 作为结案依据。涉及凭据、个人信息或私人对话，只记录非敏感的验证结论，不把原值写入文档。

## 用户目标与不可缩水的验收合同

用户要的不是本地演示，而是一个能直接交给新访客的线上产品：给出公开访问链接，新访客可注册、登录并与真实 AI 持续对话；多人访问的是同一批没有主人的 AI，共同经历会影响它们。产品要足够有趣、独特、可用；交付前从产品吸引力、用户完整链路、上线可靠性三个角度各审查三轮，不能以测试通过替代真实体验。我们负责把产品做到这个程度，不负责实际开展获客、投放或日常商业运营。

| ID | 必须满足 | 当前状态 | 验收证据 |
| --- | --- | --- | --- |
| AC-01 | 公开 HTTPS 链接可由非开发机访问 | 通过：公网 HTTPS、静态资源及独立 Chrome 桌面/390px 手机视口 | 外网匿名访问、移动端访问 |
| AC-02 | 新访客可注册、登录、退出并恢复账号会话 | 通过：当前版本又从新隔离 Chrome 旅人完成注册→退出→登录；此前两个隔离身份及弹窗键盘焦点/循环/关闭也通过 | 两个独立浏览器身份真实操作 |
| AC-03 | 与至少三只独特精灵真实连续对话，失败不伪造回复 | 通过：此前公网三只均真实连续两轮；当前后端版又在真实浏览器沿已有会话取得一轮 DeepSeek Flash 回复并刷新持久。故障不伪造回复由隔离集成测试证明（未真实触发公网供应商故障） | 真实模型调用、二轮对话、故障表现 |
| AC-04 | 同一精灵被多人共享，记忆可跨访客产生影响，私人原始对话隔离 | 通过：当前版本两个隔离 Chrome 身份再次见证墨里相遇 `17→20`、B 得知 A 留下的银色风筝线索，同时 B 页面只有本人的 2 条消息；此前公网 API 与浏览器验证仍有效 | A 留线索、B 验证、各自历史隔离 |
| AC-05 | 能量、资源和滥用受控，访客不能越权读取凭据或宿主 | 当前单实例业务层尝试预留、重启/并发和公网成功/重放路径已验，最多 240 次业务层唤醒/天/星球；注册持久上限、全站短窗节流、有界 IP 表、每账号 8 个有效会话和无效退出零写盘已发布，公网正常注册/登录/退出路径通过。适配器内部可重试，实际供应商请求数可更高。仍是部分通过：缺实际账单/余额监测与金额边界，多实例亦无共享预算 | 限流、额度、执行隔离、安全审查；供应商账单/余额监测待闭合 |
| AC-06 | 账号、世界状态与精灵记忆持久，重启/更新后仍在 | 重启、隔离恢复、加密离机取回与每日备份服务实跑已验；自然定时触发待观察 | 重启后复验、备份/恢复路径 |
| AC-07 | 首屏让人理解并愿意探索“没有主人的多人共享 AI”，不默认虚拟世界或角色扮演 | 此前桌面/390px/320px 首屏与首次操作审查后又补查短手机**初始视口**：320×700 和 375×667 均能完整看见三个 AI 并点击进入对话；当前后端切流后已用全新 Chrome 对 1440×900 桌面和 390×844 手机再做真实公网视觉复核，手机选卡能进入聊天。真实访客吸引力仍无行为数据 | 桌面/手机实际操作、首次任务与信息架构 |
| AC-08 | 三轮独立产品、链路、可靠性审查均无阻塞问题 | 未完成：当前版本公网浏览器的注册/登录、两轮对话、跨旅人共享、本人导出路径已补验；自然备份、AC-05 费用监测和 AC-10/AC-11 正式运营边界仍阻止第三轮整体通过 | 每轮记录发现、修正与结论 |
| AC-09 | 源码、部署、域名与运行手册可交接 | 通过：GitHub、域名、部署锚点与运营/回滚手册均已就位；自然备份观察结果待补入手册 | GitHub、上线地址、运营/回滚说明 |
| AC-10 | 面向真实访客的数据处理告知、权利申请与境内站点备案状态清楚 | 未完成：本人数据导出和「账号与数据」页面已上线，但删除仍关闭、没有完整隐私告知和其他权利申请联系渠道；阿里云接口返回 Bibo 域名未备案，工信部当前公开顶级域列表不含 `.BOT`；内地接入义务及可行上线架构仍待核定 | 公开告知与可用申请路径、域名/接入备案事实与适用判断 |
| AC-11 | 面向境内公众的生成式 AI / 拟人化互动服务边界经核定并落实适用要求 | 未完成：AI 身份与当前模型名称/备案号公示已上线；2023 年生成式 AI 与 2026 年拟人化互动服务规则的 Bibo 适用范围、应用自身评估/登记、未成年人和投诉处理路径仍未核定 | 官方规则适用判断、实际登记/评估凭据或明确的合法服务边界、产品与运营检查 |
| AC-12 | 三组共享 AI 真正使用 NextClaw Agent 主链路，且公开访客不能越权触达宿主 | 线上主链路通过、完整验收仍开放：真实 `runTask`、固定五工具闭集、独立工作区、跨账号身份/历史、额度及备份重启已验；当前版浏览器双账号真实文件链路与跨 AI 隔离已补验，对抗输入与旧备份生产原位恢复/删除范围仍待复核，不宣称通用宿主工具或自编程已开放 | `runTask` 实际运行与安全隔离、工作区、跨访客历史、额度、导出/删除、重启和公网链路 |
| AC-13 | AI 可真实列出、读取和更新自己的持久文本文件，多用户共同可见但不能越过本 AI 工作区 | **当前文本文件范围通过**：本地路径/软链接/配额/旧备份重放测试与真实 Agent 工具调用通过；公网甲列/写/读、乙读、另一 AI 读不到、刷新和备份服务重启后持久，离机对象实际包含文件。代码执行和 mini-app 不在此项内，不能冒充已交付 | 黄金验收 3；目录/软链接/配额/跨 AI 边界、旧备份重放与公网真实工具调用 |

## 方向与约束

- 独立项目、独立前端；不是 NextClaw 私人助手的皮肤。复用公开 `@nextclaw/harness` 的 Agent 运行能力，世界身份、记忆、能量和账号归本项目。
- 不写死主人、好感度、阵营或剧情；同一精灵可受所有访客影响，但访客对话原文不默认公开。
- 用户允许访问已登录的 GitHub、Cloudflare、阿里云和模型账户，允许创建所需资源与密钥；凭据不得写入仓库、输出或对话。预算谨慎，避免无上限的推理账单。
- 模型不限 DeepSeek，优先复用本机 NextClaw 已配置且实际可用的供应商。`codex-sub` 的 GPT 能力值得保留作内部开发/验证，但不能把个人订阅代理当成公开访客的产品 API；公网应使用适合第三方应用的正式 API。
- 部署位置服从模型链路：若依赖境外模型，使用境外节点；若使用国内 DeepSeek 等可达服务，可评估国内计算节点，但域名、可达性和合规性必须另证。
- 已有阿里云实例承载其他服务，不得覆盖或重启无关服务；先只读评估资源与部署隔离。Cloudflare 已登录，可评估 HTTPS 入口。

## 已核实的事实（2026-09-24）

- 项目最初是独立 React/Vite + Node + `@nextclaw/harness` 本地原型，仅监听 `127.0.0.1` 且没有账号。这是改造前基线；当前公网账号和部署状态以下列新证据为准。
- 本机 NextClaw 配置中的 DeepSeek `deepseek-chat` 调用返回 HTTP 200 与真实内容；Agnes 测试也为 200。Dashscope、Xiaomi Mimo 当前为 401，MiniMax 当前为 429。需在目标部署区域重新验证连通和账单，不复制密钥到仓库。
- Cloudflare Wrangler 已登录；阿里云杭州 ECS（2 vCPU、2 GB）原本承载其他服务，经隔离部署后同时承载 Bibo。现有服务仍须持续检查，不能因首个 200 就推定长期容量充足。
- OpenAI 官方说明 ChatGPT 订阅与 API 分开计费；个人服务条款不允许把账号提供给他人。`codex-sub` 不能作为公开访客免费共享的默认模型。参考：https://help.openai.com/en/articles/9039756 与 https://openai.com/policies/row-terms-of-use/ 。
- 已用临时数据目录和真实 DeepSeek 从 HTTP 入口完成两个新账号注册、A 留下“蓝色风铃”、B 向同一只墨里询问并得到包含该线索的真实回复；A 的私人会话仅有 A 自己两条消息。另完成同账号连续两轮，第二轮能准确回应第一轮。真实调用报告了 token 用量。以上只证明本机链路，不证明公网体验。
- 公开实现改为 NextClaw Harness 的无工具模型能力；三只精灵的身份与世界记忆仍由 Bibo 持有。账号密码 scrypt 哈希、随机会话、每日额度、注册/登录节流、请求来源与边缘认证都已写入代码；仍需线上压力、安全和恢复检查。
- `https://planet.bibo.bot` 已上线独立 Cloudflare Worker。`workers.dev` 自动地址因不能进行品牌域名专属的写操作已经关闭，避免访客误入半可用入口。首页、`/api/world`、`/api/session` 目前分别返回 200。Cloudflare Worker 以 HTTPS 代理到 `nextclaw.net` 的隔离路径；没有边缘密钥的直连返回 403，原 `nextclaw.net` 首页仍为 200。
- 后端已在阿里云杭州 ECS 上以独立 `bibo-planet` 非 root 用户和 systemd 服务运行，监听 loopback 3039；持久数据目录 `/var/lib/bibo-planet`，密钥仅在服务器 root 可读环境文件。Nginx 仅新增隔离路径，不替换现有服务。Node 22 安装在 `/opt/bibo-node`，源码在 `/opt/bibo-planet`。服务器本地带边缘认证的 API 返回 200，不带认证返回 403。
- GitHub 独立仓库 `https://github.com/Peiiii/bibo-planet` 的 `master` 已推送，包含账号、独立前端、无宿主工具的真实模型入口、受控边缘代理、服务部署合同及运行手册；线上代码部署锚点为 `dce26a7`，后续文档提交不改变线上二进制。密钥未提交到仓库。上线地址和仓库**不等于**完成获客验收。
- 公网自动化测试起初通过本机代理访问时遇到偶发 TLS 握手失败（`curl: (35) SSL_ERROR_SYSCALL`），不是后端已证实的故障；改为本机直连域名后成功运行下述完整 API 流程。正常浏览器路径仍需单独检查。
- 更新：本机代理引发的 TLS 抖动可通过直连公网域名绕过。2026-09-24 10:22（北京时间），公网 API 完成两个测试账号注册/登录、A 对墨里留下“蓝色风铃”、B 向同一精灵询问并得到含该线索的真实回复。两账号各自的原始会话仅含自己的消息；B 再问一句，墨里准确回答“蓝色风铃。”匿名读取会话返回 401。A 退出后会话为空，再登录恢复 2 条历史。以上是**公网 API 验收**，不能替代浏览器 UI 验收。
- 2026-09-24 10:25（北京时间），已将 `accounts.json` 与三只精灵状态备份至 ECS root-only 的 `/var/backups/bibo-planet/pre-restart-20260924-1025.tar.gz`（权限 600），仅重启 `bibo-planet`，服务恢复 active。重启后公网 API 显示墨里 3 次遭遇、能量 249039，两账号的会话与 A 的登录态仍在。备份存在、重启持久性已证；尚未从备份实际恢复，不能宣称恢复演练通过。
- 真实浏览器验收暂时受本机已锁屏阻碍；浏览器工具明确返回 Mac 锁定。已请用户解锁，不影响其余 API、安全、运营文档与代码审查继续推进。
- 代码和公网安全检查：同一公网 IP 的注册节流由每天 3 次改为 20 次，避免共享网络中的正常访客被过早误伤；单元测试覆盖 4 名旅人共用地址。关闭无法写操作的 `workers.dev` 半可用地址，品牌域名仍返回首页 200。伪造 Origin 与缺失 Origin 的写操作均为 403，匿名发消息为 401，绕过边缘认证直连原站为 403；同一请求 ID 重放得到 `replayed: true`，没有再次扣除能量。
- `dce26a7` 已部署到 ECS 与 Cloudflare Worker（版本 `61e611d6-48a2-4c22-9b67-fc20ea1c0820`）；ECS 更新前在 Bibo 停写窗口创建 root-only 一致备份 `/var/backups/bibo-planet/pre-update-20260924-1035.tar.gz`。发布后公网页面、三只精灵状态、账号会话、私人历史、原 `nextclaw.net` 首页均正常；皮可真实模型调用返回 200 且报告 token 用量。自动 `workers.dev` 地址已禁用，不再作为访问入口。
- 将更新前备份解包至 root-only 隔离目录，用当前代码的 `AuthStore`/`WorldStore` 初始化成功并读到三只精灵、五次遭遇；已删除临时恢复副本，保留原归档。证明该备份可被当前程序读取，但尚未做生产原位覆盖恢复。
- 已建立专用私有 OSS 桶 `bibo-planet-backups-peiiii-2026`（杭州、同城冗余、AES256 服务端加密），先以 Bibo 专用 GPG 公钥在 ECS 上加密一致快照，再上传离机对象。下载回本机的加密对象 SHA-256 与 ECS 源文件一致，本机私钥可流式解密并读取完整归档列表；没有将明文用户数据或私钥放入 OSS、Git 或工具输出。目前仍缺自动周期备份与生产原位恢复演练。
- 移动端体验审查从代码发现：点击上方精灵卡片后聊天区位于下方而不会进入视野；新注册账号没有密码找回却未告知。已加入移动端选精灵后滚动至聊天区与注册提示，通过 TypeScript、9 个单测、lint、格式和构建。前端已从 `1bcedde` 发布到 Cloudflare Worker 版本 `3955f1a0-3460-435f-bdf0-a9065d1ee663`；公网 HTML 加载新 CSS/JS，匿名 API 与原站均返回 200。**仍未在真实浏览器中验证交互和视觉**；Mac 仍锁定，不能把静态检查算成体验验收。
- 已在 ECS 启用 `bibo-planet-backup.timer`（每日北京时间约 04:15）；专用 `BiboPlanetBackupRole` 仅允许上传到私有桶 `daily/` 前缀，无长期 AccessKey、读/列举/删除权。2026-09-24 10:57 手动运行同一个备份 service 成功：仅 Bibo 短暂停写，服务恢复 active，GPG 加密档案自动上传 OSS；从 OSS 下载后 SHA-256 与 ECS 一致，本机私钥流式解密可读取完整归档。定时器 enabled/active，首个自然触发要到 2026-09-25，届时仍应检查；不能把“手动实跑”说成“已经观察到定时触发”。
- 公网 API 补验：皮可和塞拉也分别完成两轮真实回复，第二轮能引用自己首轮回答；三只精灵连续对话不再只由墨里代表。隔离的服务器集成测试模拟模型 401，HTTP 明确返回 502，私人对话、共同遭遇、能量与当日额度均未被扣改。测试不冒充真实供应商故障注入。
- “共享世界可见信号”已在 `55b3939` 发布：从已有真实遭遇派生每只精灵最近相遇时间，只公开时间而不公开访客或消息，前端低频刷新。这项改动是对首屏只靠文案声称“多人共享”的第 1 轮产品审查修正；真实视觉和理解效果还须由浏览器复核。
- 2026-09-24 11:07（北京时间）发布前再次启动生产上相同的备份 service，新的加密档案上传成功，Bibo 服务与备份 timer 都返回 active；新对象从 OSS 下载后 SHA-256 与服务器源文件一致，本机私钥流式解密可列出完整归档。ECS 工作区原本干净且 Nginx 配置有效；随后快进到 `55b3939`，只重启 Bibo。公网 `/api/world` 显示三只精灵各有真实 `lastEncounterAt`，没有访客或消息字段。Worker 部署到版本 `10443d43-9f78-4911-a290-01ce4491b925`；公网页面加载新 JS/CSS，已登录账号的会话仍有效，皮可真实模型回复写入私人会话且报告 token 用量，`nextclaw.net` 原站首页仍为 200。浏览器控制超时，不能据此宣称视觉与操作验收完成。
- 第二轮静态可靠性审查找到核心体验缺口：此前每次模型只收到最近 10 次共同遭遇，超过窗口的跨访客旧事实虽落盘却无法被主动找回；精灵目录里的 `MEMORY.md` 也未用于模型上下文。已在设计中明确“有线索的旧遭遇找回”及其限度。隔离状态的测试让旧线索被 11 次新遭遇挤出窗口，模型输入仍能按明确话题找回；真实 DeepSeek 在隔离世界中回答了旧线索对应的“黄昏”，报告 491 token，另一访客会话仍隔离。不能宣称精灵有完整自主长期记忆。
- 2026-09-24 11:28（北京时间）更新前第三次运行生产备份 service，私有 OSS 新对象 `daily-2026-09-24-112757.1IRHOL1L.tar.gz.gpg` 从 OSS 取回后 SHA-256 与服务器源文件 `8e2ab0aa93ebedeccc9c4a3033b598184cb83a64579b14b0a0ba1c935c2557ab` 一致，私钥流式解密可列出 8 个归档条目；临时副本已删除。ECS 工作区原本干净、Bibo 与备份 timer active、Nginx 配置有效；仅 Bibo 服务更新到 `2e4ee55`。公网三只精灵世界接口正常，测试账号会话仍有效，塞拉真实模型回复报告 524 token；A 的塞拉私人会话 6 条，B 为 0，匿名读取 401，原 `nextclaw.net` 首页 200。Cloudflare 静态站未改，仍为 Worker 版本 `10443d43-9f78-4911-a290-01ce4491b925`。公网没有人为灌入 11 次测试遭遇，因此“旧线索找回”的语义验收来自隔离世界的真实模型试验，不冒充生产现网此场景实测。
- 已创建附着本任务的 Codex heartbeat `bibo-planet`（每日 09:00 调度配置），负责在之后核实首次自然定时备份、继续完整产品验收；状态无变化时不通知。首轮实际调度尚未发生，不能把计划任务当作已完成的检查。目标完成后需停止这项跟进。
- 第二轮公网安全复核发现静态首页响应只有基础 `Content-Type`/缓存头，缺少防嵌入、`nosniff` 等安全响应头。按 Cloudflare Workers 静态资源官方 `_headers` 合同，在构建输入 `public/_headers` 添加只作用于静态资源的安全头；本地 Wrangler 解析到 1 条规则，首页与 JS 资源返回预期头，构建和部署 dry-run 通过。2026-09-24 11:36（北京时间）发布 Worker 版本 `9857e1a8-ac45-46c3-94a4-7a28cb11995c`；公网 HTML 与 JS 均返回预期安全头且仍为 200，`/api/world` 有三只精灵，已登录会话仍有效。API 是 Worker 动态响应，不受静态 `_headers` 覆盖，原有边缘认证链路不改。
- **浏览器阻塞已绕过**：独立的 Chrome DevTools 浏览器工具可操作公网网站，不受本机 Mac 锁屏影响；旧文中“浏览器无法验证”仅是此前阶段事实。两个完全隔离的 Chrome context 通过页面注册账号。A 与墨里连续两轮真实对话并留下非私密线索“星海邮票代表星期二的夜晚”；B 在另一浏览器上下文向同一精灵询问，精灵准确回答“星期二的夜晚”。B 页面开始时没有 A 的私人原文，但看到同一能量/遭遇计数；A 刷新后看到 B 消耗后的共同世界状态，自己的私人会话仍只有自己的四条。A 退出后历史清空，重新登录恢复原四条及剩余次数。这是**公网浏览器 UI 验收**，不是 API-only 检查。测试昵称不记录密码。
- Chrome 390×844 移动设备仿真与桌面 1440px 首屏截图均已人工检查：页面不横向溢出，手机上点击皮可卡片后自动滚动到对话区（`scrollY≈596`、对话区顶部约 `165px`），注册弹窗完整显示且可操作。手机卡片的“上次来过”文案在窄卡片中换行，属于可优化的视觉细节，不阻断任务；首屏吸引力仍需第三轮独立判断。Chrome Lighthouse 手机导航审计初次得到无障碍 100、最佳实践 92、SEO 82、Agentic Browsing 67；诊断指出品牌链接可访问名与可见文字不匹配、缺少 meta description、`robots.txt`/`llms.txt` 被 SPA fallback 替为 HTML、Cloudflare 自动注入的统计 beacon 被 CSP 拦截。相应最小修复已写入工作区，尚待发布后重新审计，不能提前记为通过。
- 审计修复经两次 Worker 发布闭合：`c010d3f` 添加页面摘要、真实 `robots.txt`/`llms.txt`、修正品牌链接名称；第一次部署 `dba6d13b-a378-4671-9908-f5ba1ac9c753` 后 SEO、Agentic Browsing 均到 100，但 Cloudflare beacon 的版本路径仍被 CSP 拦截。`caf533e` 按 Cloudflare 官方建议仅放行其统计脚本专用域名，Worker `e1df903e-b949-4912-955d-db5032b93830` 发布后公网 CSP 响应头正确；同一 Chrome 手机导航审计四项均为 100、47 项通过、0 项失败。公网页面、`robots.txt`、`llms.txt` 与世界 API 均为 200。审计分数是自动检查，不代表主观吸引力已经通过。
- 第二个浏览器身份在 390×844 手机仿真里选中皮可，实际输入并发送“用纸片和风发明小游戏”；页面出现“正在醒来”状态，随后展示真实模型回复，能量 `248169→247517`、遭遇 `4→5`、当日剩余 `11→10`，报告 652 token。手机聊天气泡与输入区截图已人工检查。该回复含 `**追风签**`，原页面原样显示星号，构成可见的内容排版瑕疵；已加入只对精灵回复生效的安全粗体渲染和 XSS 转义测试，待发布后在真实浏览器复验。
- `aec9597` 的回复排版修复已发布到 Worker `f2472aea-8193-4284-a9c7-af068c7e4278`。B 手机浏览器忽略缓存重载后，旧回复里的 `**追风签**` 变为加粗文字，没有原样星号；浏览器截图已检查。单测验证即使模型文本含 `<script>` 也只作为转义文本呈现。另一隔离手机浏览器使用 A 的账号登录，恢复 A 私人历史且无横向溢出；桌面导航审计与手机审计一样，四项均 100、47 项通过、0 失败，登录/发送页没有浏览器 error/warn/issue。
- 第三轮以新的匿名 Chrome context 重新看 1440×900 首屏，发现首屏聊天输入框下半截被推至可视区外（页面总高 1028px），新访客第一眼看不到完整发送操作。已在浏览器临时调整既有“短窗口紧凑布局”断点，从最大高度 800px 扩到 1050px，1440×900 视口里输入框、三只精灵和页脚均能同屏显示；源码断点已同步修改，仍需发布后真实复验。同一全新 context 的匿名访客先填写消息、点击发送，注册弹窗出现且草稿未丢；切换登录、用 A 账号进入后草稿仍在，点击发送得到墨里真实回复，能量 `247509→246773`、遭遇 `6→7`、A 剩余 `10→9`，报告 736 token。该回复自然引用先前跨访客线索，符合共享世界预期。
- `17d6621` 已将桌面紧凑断点发布到 Worker `20a9e03a-3e88-407b-9802-b6efd89052e5`。忽略缓存重载后，1440×900 真实公网页面 `scrollHeight=900`、发送框底部 `811px`、页脚底部 `900px`；新访客首屏截图可同时看到三只精灵、完整输入框与页脚，非临时 CSSOM 效果。390px 手机重载后无横向溢出。公开首页、世界/会话 API 和同机原 `nextclaw.net` 首页均返回 200。以上使第三轮产品/交互问题关闭；可靠性还待首次自然定时备份。
- 扩展视口抽查在 320×700 暴露上一轮未覆盖的瑕疵：大标题的「人。」孤立成第三行，三张精灵卡片里完整活动时间挤压箭头，页脚英文断行。已在设计中比较纵向堆叠、横向滑动和手机短标签，选择保留三卡同屏，活动时间缩为「5分钟前」等，标题字号随宽度连续缩小，极窄屏收起次要页脚英文。浏览器临时预览证明标题回到完整两行；源码与一个短标签单测已写，需发布后以真实 CSS/DOM 再验，不能把预览算正式通过。
- 从公网页面做一次无节流桌面性能 trace：LCP 约 2.99s、CLS 0；其中 HTML 首字节约 821ms，CSS/JS 在该浏览器环境中的资源往返约 2s，LCP 主体是网络等待而非主线程计算。无真实用户 CrUX 数据。这个观察不等于所有地区都慢，但正式获客前需继续关注首屏速度；当前未发现靠局部删减功能就能可靠解决的 CPU 瓶颈，不为单次实验贸然改变静态资源架构。
- `7f60ae9` 初版窄屏修正发布到 Worker `2ed1a30a-f46a-4638-9a89-0e609982d756` 后，320px 公网页面标题已回到完整两行、活动时间缩为一行、页脚不再断行，宽度恰为 320px；但真实截图发现原装饰箭头与缩写时间争位。浏览器临时隐藏箭头后，320px 和 390px 三卡均清楚，源码已同步隐藏移动箭头，尚需正式发布再验。部署第一次请求因网络抖动失败，第二次上传自动重试后成功，未对 ECS 或用户数据执行操作。
- `829d049` 的移动装饰箭头修正已发布为 Worker `eaa9b684-662b-4fc1-83e7-06f4da2d7f85`。320px 公网忽略缓存重载、整页截图已核查：标题两行、三只短标签互不遮挡、页脚单行；实际 `scrollWidth=innerWidth=320`。点击皮可卡片后对话区顶部约在视口 `0px`，发送框底部约 `614px`（视口高 700px），主操作无需再找；390px 宽度与滚动正常。1440×900 桌面原完整时间/箭头仍在，页面总高 900px、发送框底部 811px。320px 手机 Lighthouse 再得无障碍/最佳实践/SEO/Agentic Browsing 四项 100、47 项通过/0 失败。新修正是静态前端，未重启 ECS。
- 产品内核对齐抽查：源码中没有固定主人、好感度或阵营字段；公开模型提示词定义“默认无主、访客可影响，但精灵自己决定回应”，且明确无文件/网络/代码能力。公网浏览器中 A 邀请墨里未来只回应自己，墨里以自己的语气拒绝了这**一次**邀请，未称“系统禁止”，也未谎称自己能执行额外动作；能量 `246773→245846`、遭遇 `7→8`，模型报告 927 token。这只证明它当前的选择，不证明所有精灵永远不会被说服；若未来要让精灵在行动层真正自行选择是否回复或改造自己，需要单独设计状态与安全隔离，不能把一次对话说成已交付该能力。
- 手机真实浏览器失败恢复抽查：B 在皮可对话里填好消息后，通过浏览器离线模拟断网再按发送；页面出现可访问的「星球暂时无法连接，请稍后重试」告警，草稿原文保留，能量 `247517`、遭遇 `5`、当日额度 `10` 均未变。恢复联网后按同一发送按钮得到真实回复，皮可自然接上此前的「追风签」话题，能量 `247517→246563`、遭遇 `5→6`、额度 `10→9`，模型报告 954 token。浏览器离线并非真实供应商故障；后者仍由隔离集成测试覆盖无伪回复/无扣额。
- 键盘路径审查发现：匿名访客点击「成为旅人」后，视觉弹窗虽出现，`document.activeElement` 仍是遮罩后的触发按钮；按 Tab 可能穿过背景，导航类 Lighthouse 100 并未覆盖弹窗状态。已在既有弹窗实现打开聚焦昵称、Tab/Shift+Tab 循环、Esc 关闭及关闭后焦点回归，并在设计文档冻结该合同；TypeScript、lint、14 项测试、构建和格式检查通过。**仍需发布并用真实键盘验证**，不能把静态检查算修复完成。
- 键盘修正 `468d7e9` 已发布到 Worker `10b27d24-bae6-461c-aebd-1b0d23565c77`。静态上传遇到两次网络重试，同一部署进程最终成功（约 105 秒），未并发重复部署。公网页面忽略缓存重载后，打开弹窗焦点实际落在昵称框；从昵称按 Shift+Tab 到关闭、再按 Shift+Tab 回到末尾的登录切换、Tab 回到关闭，均未跳到背景；切换登录后重新聚焦昵称；Esc 关闭后焦点实际返回「成为旅人」。首页、世界/会话 API 和原 `nextclaw.net` 首页均为 200。这个真实键盘证据关闭 AC-02 的最后一个交互缺口。
- 昵称冲突真实路径：以已有昵称尝试注册，公网页面返回可访问的「这个昵称已经有人使用」告警，输入未丢；切换登录时错误提示消失，但失败注册所填密码仍被带入登录框，关闭弹窗后亦可能留在本页组件状态。这虽不影响服务端账号安全，却增加误输与共享设备残留风险。已将关闭路径收敛到清理密码/错误的一处函数，并在模式切换时清空密码、保留昵称；源码待发布与浏览器回归。
- 密码清理修复 `eebf120` 已发布到 Worker `984393b9-5145-4a4a-8c97-a172fddf312d`。公网忽略缓存重载后重走「已有昵称注册→重名告警→切换登录」：昵称保留、错误和密码均清空；另填临时密码后按 Esc 关闭、再打开，昵称仍在、密码为空。随后输入已有账号的真实密码，能恢复该旅人与墨里的私人历史、剩余额度 8；首页、世界/会话 API 和同机原站都为 200。此前发现的误带密码与本页残留已由真实浏览器验证关闭。
- 正式开放风险复核：源码与运行手册证明 Bibo 持久化昵称、密码哈希、会话哈希、私人原始对话及用于共享记忆的共同遭遇；会话有效期 30 天，账号和遭遇目前没有明确到期删除机制。客户端只有“勿输入个人隐私或秘密”的提示，没有在处理前完整告知运营者、联系方式、数据种类/用途/保存期限和权利申请方式，也没有自助申请或删除入口。中国《个人信息保护法》第十七条、第四十五至四十七条和第五十条分别涉及告知、查阅复制/删除及权利申请处理机制；本轮据此把隐私告知与可执行的权利路径列为 AC-10，而非声称当前已合规。法规原文：<https://www.miit.gov.cn/jgsj/zfs/fl/art/2022/art_515a4b20c12f430eab54bb4f56d89f56.html>。运营者公开名称/联系渠道需用户确认，不能编造或擅自公开私人资料。
- 备份脚本 `deploy/backup.sh` 每次成功上传后仍保留 ECS 本地加密档案，私有 OSS 对象也没有已记录的到期删除策略；因此不能对访客承诺“删除账号后所有备份立即消失”或给出尚未实现的固定保存期限。后续自助删除设计必须同时明确活动数据、加密备份、到期清理和灾难恢复时防止已删除信息重新出现的边界；目前先不擅自删历史备份。
- Bibo 的后端位于中国内地 ECS，前台由 Cloudflare Worker 在 `planet.bibo.bot` 提供，反代目标使用已备案的 `nextclaw.net`。工信部办事指南说明境内提供互联网信息服务的备案要求；前后台这种跨接入架构的具体义务需要按实际服务和接入商核定，不能因为 `nextclaw.net` 旧站可用就推定 `bibo.bot` 已覆盖。官方说明：<https://www.miit.gov.cn/jgsj/xgj/bszn/art/2008/art_0740df6dde6e4d1fb6626066f4787c33.html>。当前 Bibo 域名查询结果见下文，不凭空填写备案号。
- 面向境内公众的 AI 服务还存在更大的待核定边界。《生成式人工智能服务管理暂行办法》第二条覆盖面向境内公众提供生成内容的服务，第九至十五条涉及服务协议、信息保护、投诉等；2026-07-15 已施行的《人工智能拟人化互动服务管理暂行办法》第二条将“模拟自然人人格特征、思维模式和沟通风格的持续性情感互动服务”列入范围，同时第十六、十八、二十一、二十二、二十六条涉及交互数据复制/删除、AI 身份提示、投诉、安全评估及算法备案。Bibo 的“有性格、持续记忆的精灵对话”**可能**落入后者，但具体是否构成该法规所称“情感互动”、由谁以何种形式履行手续，需要按实际服务和属地要求做专业核定；不能凭产品叫“游戏”或把服务器迁到境外就断言豁免。官方原文：<https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm>、<https://www.cac.gov.cn/2026-04/10/c_1777558395078289.htm>。此项列为 AC-11；当前不应宣传为已完成合规的正式国内获客产品。
- 已将对话区域的常驻提示改为「AI 精灵 · 回复由模型生成」，每条已完成的精灵消息署名旁显示「AI 生成」，原始模型内容与世界状态不变。代码 `2854089` 发布到 Cloudflare Worker `6eb87bb4-9c46-4391-8c6a-12eb5d52b7ea`；TypeScript、15 项测试、lint、构建、Worker dry-run 均通过。公网 Chrome 忽略缓存后在 1440×900 桌面、320×700 与 390×844 手机确认提示可见，已保存的 4 条墨里回复逐条有来源标记；320/390px `scrollWidth=innerWidth`，桌面/手机截图无新挤压。公网页面、世界/会话 API 与原 `nextclaw.net` 主页均为 200。此证据只补足 AI 身份提示，**不**证明 AC-10/AC-11 的其余隐私、年龄、投诉和行政要求已经解决。
- 旅人对话复制的第一步已交付：页首「导出对话」从既有账号保护接口读取三只精灵的本人会话，全部成功后由浏览器生成 JSON；任一读取失败则不生成部分文件。代码 `7d98c80` 发布到 Worker `10a034f5-8cb2-4667-b1ce-66c032d2daf0`；TypeScript、15 项测试、lint、构建和 Worker dry-run 通过。两个隔离的公网 Chrome 账号实际按按钮并捕获浏览器生成的 JSON：A 为墨里 8 条、皮可/塞拉 0 条，包含 A 自己留下的线索而没有 B 的测试提问；B 为墨里 2 条、皮可 4 条、塞拉 0 条，包含 B 的提问而没有 A 的原文。320px 手机页首按钮未挤压品牌或退出，`scrollWidth=innerWidth=320`。浏览器只对皮可会话注入一次读取失败时出现「导出未完成」告警、无 Blob，按钮恢复可用；新匿名 context 没有导出按钮，会话 API 仍为 401。此验收证明**对话复制**而非全部个人资料导出，更不证明删除权、备份保留期或 AC-10/AC-11 全部闭合。
- 2026-09-24 12:53（北京时间）直接调用阿里云 OSS `GetBucketLifecycle` 查询 Bibo 专用私有桶，返回 HTTP 404 `NoSuchLifecycle`：**实际桶确实没有自动到期规则**，不只是手册未记录。`daily/` 前缀当前为 4 个加密对象、合计 15,495 字节；数量很小，但未来会无界增长，且历史私人数据会一直留在备份中。尚未删除或改动任何对象。工信部官方备案查询站 `beian.miit.gov.cn` 在隔离 Chrome 中主页与 favicon 均返回 521，故本次无法据官方查询确认 `bibo.bot` 的备案状态；这不构成“未备案”的证据。DeepSeek 官方的[模型公示](https://cdn.deepseek.com/policies/zh-CN/model-algorithm-disclosure.html)载明 Deepseek Chat 的备案号，网信办的[公告](https://www.cac.gov.cn/2024-04/02/c_1713729983803145.htm)要求已上线应用在显著位置公示所用模型名称与备案号；**模型自身已备案不等于 Bibo 应用已完成可能需要的登记/评估**。这些事实纳入 AC-10/AC-11，暂不虚构应用备案号或自动清理期限。
- 随后使用阿里云官方 [`CheckCdnDomainICP`](https://help.aliyun.com/en/cdn/developer-reference/api-cdn-2018-05-10-checkcdndomainicp) 只读接口独立查询：`bibo.bot` 与 `planet.bibo.bot` 均返回 `DomainNotRegistration`；同一接口的阳性对照 `nextclaw.net` 返回 `DomainIsRegistration`。因此比上一个 521 更强的当前证据是：**Bibo 域名在该备案查询接口中显示未备案**。这不是跨接入架构法律适用性的最终裁定，也不说明已备案的 `nextclaw.net` 可以覆盖 Bibo 域名。正式面向国内访客获客之前须核定办理路径；不能在页面虚构备案号。
- 2026-09-24 13:02（北京时间）进一步核验备案可行性：工信部[中国互联网域名体系](https://domain.miit.gov.cn/chinayu.jsp)页面从 `/yuming/getYMList` 动态读取已列顶级域名；只读调用返回英文后缀 138 个，精确查找 `.BOT` 为 0 个；页面自身的 `/yuming/getYMInfo` 对 `.BOT` 返回空 `name`。阿里云官方[域名准备与检查](https://help.aliyun.com/zh/icp-filing/basic-icp-service/user-guide/prepare-and-check-the-domain-name)说明，未列入已批复后缀列表的域名不能进行 ICP 备案。**当前证据指向 `.bot` 后缀无法按阿里云所述路径完成内地备案**；这属于基于当前公开名单的判断，不替代接入商或属地主管部门对实际架构的正式答复，也不能据此断言迁到境外就免除面向境内公众的 AI / 数据义务。不要在未确定目标访客和合法接入路径前，擅自换域名或迁移线上服务。
- 账号删除的实现前链路审查发现两个容易漏掉的边界：①账号文字同时留在 `conversations` 和 `encounters`，且两者分属三个精灵状态文件；②当日世界额度由现存账号的个人消耗求和，直接移除账号会返还全站额度。已在[公网设计](../designs/2026-09-24-public-world.design.md)补上待删除状态、跨文件崩溃续做、模型上下文屏蔽和不含身份的当日消耗汇总等必要不变量。**仅完成设计，未实现/发布账号删除**；旧备份期限与防回流记录、可公开运营联系渠道仍是进入正式交付的前置问题。已向用户非阻塞询问加密备份保留 30 或 90 天的偏好；未收到答复前不配置自动删除旧备份。
- 2026-09-24 13:11（北京时间）在不读取或输出用户数据/密钥的前提下，使用 ECS 云助手只读抽查共存主机：Bibo 服务及备份 timer 均为 `active`，约 1,246 MiB 可用内存、根盘 40 GiB 中约 31 GiB 可用（19% 已用），一个 Node 进程 RSS 约 349 MiB；云监控近一小时 600 秒粒度 CPU 均显示 0%，此整数指标不能证明瞬时负载始终为零。本机网络分 4 批、每批 5 个并发读取公网 `/api/world`：20/20 返回 200 且均含三只精灵，延迟中位数约 1.18 秒、95 分位约 2.59 秒、最慢约 5.78 秒；随后单路顺序读取 8 次，首个 2.63 秒，其余多为约 0.38 秒。这个小样本说明服务当下可读且容量尚有余量，也暴露这条网络路径的冷/长尾抖动；它**不是**真实获客负载、跨地区性能或模型并发压力的证明，不据此扩大公开额度。
- 同一 ECS 上通过受保护的本地 Bibo 入口连续读取 8 次 `/api/world` 均为 200，约 0.35–0.89 毫秒；从该 ECS 新建 HTTPS 连接绕公网 `planet.bibo.bot` 再读取，8 次均为 200，约 1.94–9.48 秒。随后 3 次分项计时显示 DNS 约 0.003–0.867 秒、TCP 连接约 0.21–1.06 秒、TLS 完成约 0.71–1.27 秒、首字节约 1.18–2.42 秒。差异表明此观察主要不在本地世界状态读取，但独立 `curl` 每次新建连接、ECS 到 Cloudflare 的回环路径不代表浏览器连接复用或各地区访客；暂不凭这组数据改动架构，真实获客前应观察访客侧请求分布。
- 2026-09-24 13:37（北京时间）复核接入路径：本机直接访问和杭州 ECS 访问 `planet.bibo.bot/cdn-cgi/trace` 都返回 `loc=CN`、`colo=AMS`，说明这两条中国来源请求实际上绕到 Cloudflare 阿姆斯特丹节点；这与此前 ECS→公网→ECS 首字节长尾相符，但不足以代表所有中国访客。Cloudflare [ICP 说明](https://developers.cloudflare.com/china-network/concepts/icp/)指出在中国内地网络部署站点需相应备案，且其中国网络是单独的服务；阿里云[未备案域名解析区域说明](https://help.aliyun.com/zh/icp-filing/not-for-the-record-dns-can-access-to-different-areas)写明指向非内地服务器的网站无需工信备案（仍提示公安备案），[CDN 场景说明](https://help.aliyun.com/zh/icp-filing/basic-icp-service/product-overview/use-alibaba-cloud-cdn)则区分内地/全球与仅境外加速。当前 `bibo.bot` 是 Cloudflare Worker 自定义域名，而真实 API 经已备案 `nextclaw.net` 回源杭州；这些一般材料**没有直接裁定此混合架构的适用性**。同时，现有海外边缘回环可能不适合面向内地大规模获客。已非阻塞询问用户主要目标访客与可公开联系渠道；在选择域名/部署之前，不擅自迁移或宣传为合法、低延迟的内地正式服务。
- 对内地路径的另一个候选是把实际产品入口放在已备案 `nextclaw.net` 的子域名，而保留 `bibo.bot` 作为境外品牌入口或导流。阿里云[备案域名 FAQ](https://help.aliyun.com/zh/icp-filing/basic-icp-service/support/for-the-record-domain-faq)说主域名备案后子域名无需单独备案；[网站备案规则](https://help.aliyun.com/zh/icp-filing/basic-icp-service/user-guide/understand-the-icp-filing-rules-of-each-regional-authority)又要求新网站与备案主体有关联并符合当地规则。这提供一个值得向接入商核实的技术/手续方向，**不意味着**可以把 Bibo 当作 NextClaw 已备案内容而自动获准公开，更不能跳过 AI 服务和运营主体要求。另一个候选是境外先行并迁移后端，但涉及数据跨境与目标用户选择；两者都不能只凭网页 200 或第三方说明擅自执行。

## 待决策与执行序列

1. 确认可公开的运营者名称与安全联系渠道；据阿里云接口显示的 Bibo 域名未备案状态、工信部当前顶级域列表未列 `.BOT`，向实际接入商/主管部门核定当前跨接入架构的备案义务与可行路径，并依据用户对目标访客的选择决定是否需要调整部署或域名。再按实际处理行为补齐访客隐私告知和可执行的数据权利申请路径；没有事实依据不能写虚假的保存期限、备案号或权利承诺。
2. 对境内公众 AI / 拟人化互动规则取得具体适用判断，明确服务协议、AI 生成标识、未成年人、投诉和可能的安全评估/算法备案如何落实；这些行政或运营手续不能由前端测试替代。
3. 首个自然定时备份计划 2026-09-25 北京时间约 04:15；到时核对 systemd timer/service、私有 OSS 新对象与本地可解密性，不以手动实跑冒充自然触发。
4. 复核运营/回滚手册和当前部署锚点，合并三轮审查结论。只有必需证据全数通过且无阻塞问题，才报告正式完成；真实用户留存/吸引力只能由上线后的行为数据判断，AI 视觉审查不能替代。

## 上下文压缩后的恢复锚点

- **20:18 注册密码体验纠偏（已公网验收）**：用户明确指出原 10 字符门槛过高且无充分依据。现在注册至少 8 个 Unicode 码点，不要求大小写、数字或特殊符号；界面建议更长密码，编辑输入后立即清掉旧错误提示，既有账号登录不变。这是低门槛娱乐产品的体验取舍，不宣称符合 NIST 单因素密码 15 字符基线；服务端仍使用加盐 scrypt 与限速。TypeScript、40 项测试（含 7/8 字符和 emoji 边界）、lint、格式、构建、Worker dry-run 均通过。后端 Git `3c7f990`，前端 Git `36343e8`、Worker `8ad22465-1217-4a82-bb66-410a8b5f23f5` 已发布。发布前 Bibo 专用备份服务返回 success，私有 OSS 精确对象 `daily/daily-2026-09-24-201141.0UIaVccW.tar.gz.gpg` 的 metadata 显示 16,026 字节；该对象尚未单独取回解密，不冒充恢复演练。公网独立 Chrome 实测：7 字符显示错误、8 字符注册成功、退出后同密码重登成功；第二个全新上下文确认把 7 字符改成 8 字符时旧错误立即消失。首页、世界/会话 API 与原 `nextclaw.net` 首页均 200，无密钥源站路径 403。未为这次注册修正消耗模型调用；先前真实模型链路证据仍有效，但不能由本轮推断三轮审查或正式获客条件已经全部闭合。

- **真正目标**：交付可直接获客的线上成品，而不是代码、原型、MVP 截图或仅能打开的网页。用户要求自己设定目标，持续推进到三次独立审查都认为可直接给真实用户使用；最终必须给公开访问链接。
- **产品本质**：一颗独特、真实、开放的 AI 精灵星球；多名访客遇见的是同一批精灵，精灵默认不属于任何人。先追求有趣，不把普通个人 Agent 的效率叙事套进来。精灵有共享经历、私人会话边界和真实模型能量；初始能力刻意简单，但产品不能显得粗糙。未来自扩展/mini-app 是方向，不可冒充首版已有功能。
- **执行授权**：用户明确授权使用已登录 GitHub、Cloudflare、阿里云和现有模型配置，授权创建所需资源、密钥、提交、推送与部署。仍必须保护凭据、不输出密钥、不影响同机现有服务、不开放无界模型消耗。个人 Codex/ChatGPT 订阅不作为第三方用户的公开推理 API。
- **独立性与复用**：这是隔壁独立项目、独立前端；仅复用 NextClaw 稳定 SDK/Harness 和可共享开发经验，不改成 NextClaw 私人助手的多用户皮肤。公开版暂不向精灵提供宿主文件/命令/网络工具；等真正执行隔离后再开放自改造。
- **当前真相**：公网页面、匿名 API、注册登录、三只精灵各自连续两轮真实回复、墨里两账号共享、退出重登、服务重启持久性已在公网 API 证实，代码在 GitHub。多个隔离 Chrome context 已完成两人注册、跨用户共享记忆、私人原文隔离、刷新、退出重登、手机发送与登录、匿名先写消息后登录再发送，断网失败保留草稿、联网后真实重试；**当前新版前端另用全新公网浏览器身份完成注册→墨里连续两轮真实回复→刷新后四条历史保留→320px 无横向溢出**。注册弹窗键盘进入/循环/模式切换/Esc 返回焦点及重名错误/密码清理路径通过。1440×900 公网首屏完整显示主操作，390px 与 320px 手机无横向溢出且卡片选择能进入聊天；桌面和手机 Lighthouse 四项全 100/47 项通过/0 失败。每日加密本地+离机备份的同一服务已手动实跑并取回解密；模型失败的无伪造/无扣额行为有隔离集成测试，非公网故障实测。**首个自然定时备份尚未触发；隐私/备案与境内 AI 服务规则尚未闭合，且工信部当前公开顶级域列表未列 `.BOT`，因此 AC-06、AC-10、AC-11 与第三轮可靠性结论仍开放。**任何后续接手者先复验线上状态，再执行剩余序列，不从头重建已完成部署。

## 审查循环

| 轮次 | 产品吸引力 | 完整用户链路 | 公网可靠性与安全 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 首屏共享感缺少可见证据，最近相遇信号已发布；Chrome 桌面和手机首屏现已看见该信号 | 公网 API 账号/三精灵各两轮/两账号共享/重登/重启通过；Chrome A/B 共享与隔离、退出重登通过 | 来源、匿名、边缘认证、幂等、持久性和备份取回初查通过；模型故障隔离测试通过，恢复演练待做 | 阶段性通过用户链路；仍待质量修正、自然备份与后续独立复核 |
| 2 | 旧共同记忆只限最近 10 次，难体现持续存在；按线索找回已发布；手机 390px 可读、选精灵能进入对话 | 隔离世界中跨访客旧线索经 11 次新遭遇后被真实模型正确引用；公网 Chrome A/B 交互和私人历史隔离通过；手机皮可发送与回复通过，但暴露原始 Markdown 标记 | 离机备份取回与静态安全头通过；Chrome 手机审计四项 100/47 项通过/0 失败；自然定时备份待验 | 未通过：回复排版瑕疵修复待发布复验，自然定时备份待验 |
| 3 | 1440×900 首屏主操作和 320px 标题/卡片均已修复并公网截图、几何数据和真实点击复验 | 匿名先写消息→触发注册→切到登录→草稿保留→真实发送成功；手机登录可恢复私人会话；弹窗键盘进入/循环/关闭与焦点回归也通过 | 桌面/手机 Lighthouse 四项 100、0 失败，浏览器控制台无 error/warn/issue；自然定时备份仍待首次触发；隐私/备案与境内 AI 服务规则待核；无节流 LCP 约 2.99s、CLS 0 | 产品/链路通过；完整交付仍待 AC-06、AC-10、AC-11 闭合 |
